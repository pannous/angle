!!python/object/apply:collections.defaultdict
args: [!!python/name:__builtin__.list '']
dictitems:
  BeautifulSoup: [buildTagMap, chardet, codecs, generators, markupbase, name2codepoint,
    re, sgmllib]
  CoreFunctions: [download, json, urllib2]
  Cython: [Py_UCS4, Py_UNICODE, Py_ssize_t, Shadow, address, bint, ccall, cclass,
    cdiv, cfunc, cmod, compiled, complex, complex_types, declare, double, doublecomplex,
    final, float, float_types, floatcomplex, floating, fused_type, gil, gs, index_type,
    inline, int, internal, load_ipython_extension, locals, long, longdouble, longdoublecomplex,
    longlong, no_gc_clear, nogil, numeric, other_types, pointer, py_complex, py_float,
    py_int, py_long, reprname, returns, schar, short, sint, size_t, sizeof, slong,
    slonglong, sshort, test_assert_path_exists, test_fail_if_path_exists, to_repr,
    type_ordering, type_version_tag, typeof, uchar, uint, ulong, ulonglong, unicode,
    ushort, void]
  PIL: [Image, ImageFilter, ImageMode]
  StringIO: [test]
  abc: [abstractmethod]
  aifc: [openfp, struct]
  altgraph: [pkg_resources]
  antigravity: [webbrowser]
  ast: [PyCF_ONLY_AST, copy_location, dump, fix_missing_locations, get_docstring,
    increment_lineno, iter_child_nodes, iter_fields, literal_eval, parse]
  ast2json: [ast2json, get_value, parse, str2json]
  asteval: [asteval, astutils, valid_symbol_name]
  astor: [all_symbols, codegen, codetoast, dump, get_anyop, get_binop, get_boolop,
    get_cmpop, get_unaryop, iter_node, misc, parsefile, to_source, treewalk]
  asynchat: [asyncore, filterwarnings, find_prefix_at_end, py3kwarning, socket]
  asyncore: [close_all, compact_traceback, errorcode, fcntl, loop, poll, poll2, poll3,
    readwrite, select, socket, socket_map, time, warnings]
  atexit: [register]
  audioop: [add, adpcm2lin, alaw2lin, avg, avgpp, bias, cross, findfactor, findfit,
    findmax, getsample, lin2adpcm, lin2alaw, lin2lin, lin2ulaw, max, maxpp, minmax,
    mul, ratecv, reverse, rms, tomono, tostereo, ulaw2lin]
  awscli: [EnvironmentVariables]
  base64: [b16decode, b16encode, b32decode, b32encode, b64decode, b64encode, binascii,
    decode, decodestring, encode, encodestring, k, re, standard_b64decode, standard_b64encode,
    struct, test, test1, urlsafe_b64decode, urlsafe_b64encode, v]
  bdb: [bar, checkfuncname, effective, fnmatch, foo, set_trace, test]
  binascii: [a2b_base64, a2b_hex, a2b_hqx, a2b_qp, a2b_uu, b2a_base64, b2a_hex, b2a_hqx,
    b2a_qp, b2a_uu, crc32, crc_hqx, hexlify, rlecode_hqx, rledecode_hqx, unhexlify]
  binhex: [binascii, binhex, getfileinfo, hexbin, struct]
  bisect: [bisect, bisect_left, bisect_right, insort, insort_left, insort_right]
  botocore: [ScalarTypes, log, logging, re, xform_name]
  bz2: [compress, decompress]
  cProfile: [help, label, main, run, runctx]
  calendar: [February, January, c, calendar, datetime, day_abbr, day_name, firstweekday,
    format, formatstring, isleap, leapdays, main, mdays, month, month_abbr, month_name,
    monthcalendar, monthrange, prcal, prmonth, prweek, setfirstweekday, timegm, week,
    weekday, weekheader]
  certifi: [core, where]
  cgi: [StringIO, dolog, escape, filterwarnings, initlog, log, logfile, logfp, maxlen,
    mimetools, nolog, parse, parse_header, parse_multipart, parse_qs, parse_qsl, print_arguments,
    print_directory, print_environ, print_environ_usage, print_exception, print_form,
    rfc822, test, urlparse, valid_boundary, warn]
  cgitb: [enable, grey, handler, html, inspect, keyword, linecache, lookup, pydoc,
    reset, scanvars, small, strong, tempfile, text, time, tokenize, traceback]
  climate: [add_arg, add_command, add_group, add_mutex, annotate, enable_default_logging,
    flags, get_logger, log, logging, parse_args, parse_known_args, plac]
  cmath: [acos, acosh, asin, asinh, atan, atanh, cos, cosh, e, exp, isinf, isnan,
    log, log10, phase, pi, polar, rect, sin, sinh, sqrt, tan, tanh]
  code: [compile_command, interact, softspace, traceback]
  codecs: [EncodedFile, ascii_decode, ascii_encode, backslashreplace_errors, charbuffer_encode,
    charmap_build, charmap_decode, charmap_encode, decode, encode, escape_decode,
    escape_encode, getdecoder, getencoder, getincrementaldecoder, getincrementalencoder,
    getreader, getwriter, ignore_errors, iterdecode, iterencode, latin_1_decode, latin_1_encode,
    lookup, lookup_error, make_encoding_map, make_identity_dict, raw_unicode_escape_decode,
    raw_unicode_escape_encode, readbuffer_encode, register, register_error, replace_errors,
    strict_errors, unicode_escape_decode, unicode_escape_encode, unicode_internal_decode,
    unicode_internal_encode, utf_16_be_decode, utf_16_be_encode, utf_16_decode, utf_16_encode,
    utf_16_ex_decode, utf_16_le_decode, utf_16_le_encode, utf_32_be_decode, utf_32_be_encode,
    utf_32_decode, utf_32_encode, utf_32_ex_decode, utf_32_le_decode, utf_32_le_encode,
    utf_7_decode, utf_7_encode, utf_8_decode, utf_8_encode, xmlcharrefreplace_errors]
  codeop: [PyCF_DONT_IMPLY_DEDENT, compile_command, fname]
  collections: [namedtuple]
  colorama: [Back, Cursor, Fore, Style, ansi, ansitowin32, deinit, init, initialise,
    reinit, win32, winterm]
  coloredlogs: [ansi_color_codes, ansi_text, copy, decrease_verbosity, find_defined_levels,
    get_level, increase_verbosity, install, is_verbose, logging, re, root_handler,
    set_level, socket, time]
  colorsys: [hls_to_rgb, hsv_to_rgb, rgb_to_hls, rgb_to_hsv, rgb_to_yiq, yiq_to_rgb]
  compileall: [compile_dir, compile_file, compile_path, expand_args, imp, main, py_compile,
    struct]
  configobj: [compiler, dquot, flatten_errors, getObj, get_extra_values, interpolation_engines,
    match_utf8, noquot, re, six, squot, tdquot, tsquot, unrepr, wspace_plus]
  contextlib: [contextmanager, nested, warn, wraps]
  copy: [PyStringMap, copy, deepcopy, dispatch_table, weakref]
  copy_reg: [add_extension, clear_extension_cache, constructor, dispatch_table, pickle,
    pickle_complex, remove_extension]
  crypt: [crypt]
  cssselect: [parse, parser, xpath]
  csv: [StringIO, field_size_limit, get_dialect, list_dialects, re, reader, reduce,
    register_dialect, unregister_dialect, writer]
  ctypes: [SetPointerType, addressof, alignment, byref, c_buffer, cast, cdll, create_string_buffer,
    create_unicode_buffer, get_errno, macholib, memmove, memset, pointer, pydll, pythonapi,
    resize, set_conversion_mode, set_errno, sizeof, string_at, util, wstring_at]
  curses: [baudrate, beep, can_change_color, cbreak, color_content, color_pair, curs_set,
    def_prog_mode, def_shell_mode, delay_output, doupdate, echo, endwin, erasechar,
    filter, flash, flushinp, getmouse, getsyx, getwin, halfdelay, has_colors, has_ic,
    has_il, has_key, init_color, init_pair, initscr, intrflush, is_term_resized, isendwin,
    keyname, killchar, longname, meta, mouseinterval, mousemask, napms, newpad, newwin,
    nl, nocbreak, noecho, nonl, noqiflush, noraw, pair_content, pair_number, putp,
    qiflush, raw, reset_prog_mode, reset_shell_mode, resetty, resize_term, resizeterm,
    savetty, setsyx, setupterm, start_color, termattrs, termname, tigetflag, tigetnum,
    tigetstr, tparm, typeahead, unctrl, ungetch, ungetmouse, use_default_colors, use_env,
    wrapper]
  cv: [Abs, AbsDiff, AbsDiffS, Acc, AdaptiveThreshold, Add, AddS, AddWeighted, And,
    AndS, ApproxChains, ApproxPoly, ArcLength, Avg, AvgSdv, BackProjectPCA, BoundingRect,
    BoxPoints, CV_BGR2Lab, CV_BGR2Luv, CV_BGR2YCrCb, CV_BayerBG2BGR, CV_BayerBG2BGR_VNG,
    CV_BayerBG2RGB, CV_BayerGB2BGR, CV_BayerGB2BGR_VNG, CV_BayerGB2RGB, CV_BayerGR2BGR,
    CV_BayerGR2BGR_VNG, CV_BayerGR2RGB, CV_BayerRG2BGR, CV_BayerRG2BGR_VNG, CV_BayerRG2RGB,
    CV_ErrModeLeaf, CV_ErrModeParent, CV_ErrModeSilent, CV_GAUSSIAN_5x5, CV_Lab2BGR,
    CV_Lab2RGB, CV_Luv2BGR, CV_Luv2RGB, CV_RGB2Lab, CV_RGB2Luv, CV_RGB2YCrCb, CV_YCrCb2BGR,
    CV_YCrCb2RGB, CalcArrBackProject, CalcArrHist, CalcBackProject, CalcBackProjectPatch,
    CalcCovarMatrix, CalcEMD2, CalcGlobalOrientation, CalcHist, CalcMotionGradient,
    CalcOpticalFlowBM, CalcOpticalFlowFarneback, CalcOpticalFlowHS, CalcOpticalFlowLK,
    CalcOpticalFlowPyrLK, CalcPCA, CalcProbDensity, CalcSubdivVoronoi2D, CalibrateCamera2,
    CalibrationMatrixValues, CamShift, Canny, CaptureFromCAM, CaptureFromFile, CartToPolar,
    Cbrt, Ceil, CheckArr, CheckContourConvexity, Circle, ClearHist, ClearND, ClearSeq,
    ClearSubdivVoronoi2D, ClipLine, CloneImage, CloneMat, CloneMatND, CloneSeq, Cmp,
    CmpS, CompareHist, ComputeCorrespondEpilines, ContourArea, Convert, ConvertImage,
    ConvertMaps, ConvertPointsHomogeneous, ConvertScale, ConvertScaleAbs, ConvexHull2,
    ConvexityDefects, Copy, CopyMakeBorder, CornerEigenValsAndVecs, CornerHarris,
    CornerMinEigenVal, CountNonZero, CreateCameraCapture, CreateData, CreateFileCapture,
    CreateHist, CreateImage, CreateImageHeader, CreateKalman, CreateMat, CreateMatHeader,
    CreateMatND, CreateMatNDHeader, CreateMemStorage, CreatePOSITObject, CreateStereoBMState,
    CreateStereoGCState, CreateStructuringElementEx, CreateSubdivDelaunay2D, CreateTrackbar,
    CreateVideoWriter, CrossProduct, CvtColor, CvtPixToPlane, CvtScale, DecodeImage,
    DecodeImageM, DecomposeProjectionMatrix, DestroyAllWindows, DestroyWindow, Det,
    Dilate, DistTransform, Div, DotProduct, DrawChessboardCorners, DrawContours, EigenVV,
    Ellipse, EllipseBox, EncodeImage, EqualizeHist, Erode, EstimateRigidTransform,
    Exp, ExtractSURF, FastArctan, FillConvexPoly, FillPoly, Filter2D, FindChessboardCorners,
    FindContours, FindCornerSubPix, FindDataMatrix, FindExtrinsicCameraParams2, FindFundamentalMat,
    FindHomography, FindNearestPoint2D, FindStereoCorrespondenceBM, FindStereoCorrespondenceGC,
    FitEllipse2, FitLine, Flip, FloodFill, Floor, Get1D, Get2D, Get3D, GetAffineTransform,
    GetCaptureProperty, GetCentralMoment, GetCol, GetCols, GetDiag, GetDims, GetElemType,
    GetHuMoments, GetImage, GetImageCOI, GetImageROI, GetMat, GetMinMaxHistValue,
    GetND, GetNormalizedCentralMoment, GetOptimalDFTSize, GetOptimalNewCameraMatrix,
    GetPerspectiveTransform, GetQuadrangleSubPix, GetReal1D, GetReal2D, GetReal3D,
    GetRealND, GetRectSubPix, GetRotationMatrix2D, GetRow, GetRows, GetSize, GetSpatialMoment,
    GetStarKeypoints, GetSubRect, GetTextSize, GetTickCount, GetTickFrequency, GetTrackbarPos,
    GetWindowProperty, GoodFeaturesToTrack, GrabCut, GrabFrame, HOGDetectMultiScale,
    HaarDetectObjects, HoughCircles, HoughLines2, InRange, InRangeS, InitFont, InitIntrinsicParams2D,
    InitLineIterator, InitUndistortMap, InitUndistortRectifyMap, Inpaint, Integral,
    InvSqrt, Invert, IsInf, IsNaN, KMeans2, KalmanCorrect, KalmanPredict, Laplace,
    Line, Load, LoadImage, LoadImageM, Log, LogPolar, Mahalonobis, MatMul, MatMulAdd,
    MatchShapes, MatchTemplate, Max, MaxRect, MaxS, MeanShift, Merge, Min, MinAreaRect2,
    MinEnclosingCircle, MinMaxLoc, MinS, MixChannels, Moments, MorphologyEx, MoveWindow,
    Mul, MulSpectrums, MulTransposed, MultiplyAcc, NamedWindow, Norm, Normalize, NormalizeHist,
    Not, Or, OrS, PerspectiveTransform, PointPolygonTest, PolarToCart, PolyLine, Pow,
    PreCornerDetect, ProjectPCA, ProjectPoints2, PutText, PyrDown, PyrMeanShiftFiltering,
    PyrSegmentation, PyrUp, QueryFrame, QueryHistValue_1D, QueryHistValue_2D, QueryHistValue_3D,
    QueryHistValue_nD, RQDecomp3x3, RandArr, RandInt, RandReal, RandShuffle, Range,
    RealScalar, Rectangle, Reduce, Remap, Repeat, ReprojectImageTo3D, ResetImageROI,
    Reshape, ReshapeMatND, Resize, ResizeWindow, RetrieveFrame, Rodrigues2, Round,
    RunningAvg, SVBkSb, Save, SaveImage, Scalar, ScalarAll, Scale, ScaleAdd, SegmentMotion,
    SeqInvert, SeqRemove, SeqRemoveSlice, Set, Set1D, Set2D, Set3D, SetCaptureProperty,
    SetData, SetIdentity, SetImageCOI, SetImageROI, SetMouseCallback, SetND, SetReal1D,
    SetReal2D, SetReal3D, SetRealND, SetTrackbarPos, SetWindowProperty, SetZero, ShowImage,
    Smooth, SnakeImage, Sobel, Solve, SolveCubic, SolvePoly, Sort, Split, Sqrt, SquareAcc,
    StartWindowThread, StereoCalibrate, StereoRectify, StereoRectifyUncalibrated,
    Sub, SubRS, SubS, Subdiv2DEdgeDst, Subdiv2DEdgeOrg, Subdiv2DGetEdge, Subdiv2DLocate,
    Subdiv2DNextEdge, Subdiv2DRotateEdge, SubdivDelaunay2DInsert, Sum, ThreshHist,
    Threshold, Trace, Transform, Transpose, Undistort2, UndistortPoints, UpdateMotionHistory,
    WaitKey, WarpAffine, WarpPerspective, Watershed, WriteFrame, Xor, XorS, Zero,
    fromarray, mGet, mSet, temp_test]
  cv2: [Algorithm__create, Algorithm_getList, BFMatcher, BOWImgDescriptorExtractor,
    BOWKMeansTrainer, BackgroundSubtractorMOG, BackgroundSubtractorMOG2, Boost, CamShift,
    Canny, CascadeClassifier, DMatch, DTree, DescriptorExtractor_create, DescriptorMatcher_create,
    ERTrees, FastFeatureDetector, Feature2D_create, FeatureDetector_create, FileNode,
    FileStorage, FlannBasedMatcher, GBTrees, GFTTDetector, GaussianBlur, GridAdaptedFeatureDetector,
    HOGDescriptor, HOGDescriptor_getDaimlerPeopleDetector, HOGDescriptor_getDefaultPeopleDetector,
    HoughCircles, HoughLines, HoughLinesP, HuMoments, KDTree, KNearest, KalmanFilter,
    KeyPoint, Laplacian, Mahalanobis, NormalBayesClassifier, PCABackProject, PCACompute,
    PCAComputeVar, PCAProject, PyramidAdaptedFeatureDetector, RQDecomp3x3, RTrees,
    Rodrigues, SVBackSubst, SVDecomp, Scharr, SimpleBlobDetector, SimpleBlobDetector_Params,
    Sobel, StarDetector, StereoBM, StereoSGBM, StereoVar, Subdiv2D, VideoCapture,
    VideoWriter, absdiff, accumulate, accumulateProduct, accumulateSquare, accumulateWeighted,
    adaptiveBilateralFilter, adaptiveThreshold, add, addWeighted, applyColorMap, approxPolyDP,
    arcLength, arrowedLine, batchDistance, bilateralFilter, bitwise_and, bitwise_not,
    bitwise_or, bitwise_xor, blur, borderInterpolate, boundingRect, boxFilter, buildOpticalFlowPyramid,
    calcBackProject, calcCovarMatrix, calcGlobalOrientation, calcHist, calcMotionGradient,
    calcOpticalFlowFarneback, calcOpticalFlowPyrLK, calcOpticalFlowSF, calibrateCamera,
    calibrationMatrixValues, cartToPolar, chamerMatching, checkHardwareSupport, checkRange,
    circle, clipLine, compare, compareHist, completeSymm, composeRT, computeCorrespondEpilines,
    contourArea, convertMaps, convertPointsFromHomogeneous, convertPointsToHomogeneous,
    convertScaleAbs, convexHull, convexityDefects, copyMakeBorder, cornerEigenValsAndVecs,
    cornerHarris, cornerMinEigenVal, cornerSubPix, correctMatches, countNonZero, createCLAHE,
    createEigenFaceRecognizer, createFisherFaceRecognizer, createHanningWindow, createLBPHFaceRecognizer,
    createTrackbar, cubeRoot, cv, cvtColor, dct, decomposeProjectionMatrix, destroyAllWindows,
    destroyWindow, determinant, dft, dilate, distanceTransform, distanceTransformWithLabels,
    divide, drawChessboardCorners, drawContours, drawDataMatrixCodes, drawKeypoints,
    eigen, ellipse, ellipse2Poly, equalizeHist, erode, estimateAffine3D, estimateRigidTransform,
    exp, extractChannel, fastAtan2, fastNlMeansDenoising, fastNlMeansDenoisingColored,
    fastNlMeansDenoisingColoredMulti, fastNlMeansDenoisingMulti, fillConvexPoly, fillPoly,
    filter2D, filterSpeckles, findChessboardCorners, findCirclesGrid, findCirclesGridDefault,
    findContours, findDataMatrix, findFundamentalMat, findHomography, findNonZero,
    fitEllipse, fitLine, flann_Index, flip, floodFill, gemm, getAffineTransform, getBuildInformation,
    getCPUTickCount, getDefaultNewCameraMatrix, getDerivKernels, getGaborKernel, getGaussianKernel,
    getNumberOfCPUs, getOptimalDFTSize, getOptimalNewCameraMatrix, getPerspectiveTransform,
    getRectSubPix, getRotationMatrix2D, getStructuringElement, getTextSize, getTickCount,
    getTickFrequency, getTrackbarPos, getValidDisparityROI, getWindowProperty, goodFeaturesToTrack,
    grabCut, groupRectangles, hconcat, idct, idft, imdecode, imencode, imread, imshow,
    imwrite, inRange, initCameraMatrix2D, initModule_nonfree, initUndistortRectifyMap,
    initWideAngleProjMap, inpaint, insertChannel, integral, integral2, integral3,
    intersectConvexConvex, invert, invertAffineTransform, isContourConvex, kmeans,
    line, log, magnitude, matMulDeriv, matchShapes, matchTemplate, max, mean, meanShift,
    meanStdDev, medianBlur, merge, min, minAreaRect, minEnclosingCircle, minMaxLoc,
    mixChannels, moments, morphologyEx, moveWindow, mulSpectrums, mulTransposed, multiply,
    namedWindow, norm, normalize, patchNaNs, perspectiveTransform, phase, phaseCorrelate,
    phaseCorrelateRes, pointPolygonTest, polarToCart, polylines, pow, preCornerDetect,
    projectPoints, putText, pyrDown, pyrMeanShiftFiltering, pyrUp, randShuffle, randn,
    randu, rectangle, rectify3Collinear, reduce, remap, repeat, reprojectImageTo3D,
    resize, resizeWindow, scaleAdd, segmentMotion, sepFilter2D, setIdentity, setMouseCallback,
    setTrackbarPos, setUseOptimized, setWindowProperty, solve, solveCubic, solvePnP,
    solvePnPRansac, solvePoly, sort, sortIdx, split, sqrt, startWindowThread, stereoCalibrate,
    stereoRectify, stereoRectifyUncalibrated, subtract, sumElems, threshold, trace,
    transform, transpose, triangulatePoints, undistort, undistortPoints, updateMotionHistory,
    useOptimized, validateDisparity, vconcat, waitKey, warpAffine, warpPerspective,
    watershed]
  datetime: [datetime_CAPI]
  dateutil: [parser, relativedelta, rrule, tz]
  dbm: [library]
  debugger: [logging, merge_debug_metaclass_with, re, time, traceback]
  decimal: [BasicContext, DefaultContext, ExtendedContext, getcontext, localcontext,
    setcontext]
  decorator: [contextmanager, decorator, get_init, inspect, re]
  difflib: [context_diff, get_close_matches, heapq, ndiff, reduce, restore, unified_diff]
  dis: [disassemble, disassemble_string, disco, distb, findlabels, findlinestarts]
  distutils: [archive_util, ccompiler, cmd, command, config, core, cygwinccompiler,
    debug, dep_util, dir_util, dist, errors, extension, fancy_getopt, file_util, filelist,
    log, msvccompiler, spawn, sysconfig, unixccompiler, util]
  docopt: [docopt, extras, formal_usage, parse_argv, parse_atom, parse_defaults, parse_expr,
    parse_long, parse_pattern, parse_seq, parse_shorts, printable_usage, re]
  doctest: [DocFileSuite, DocFileTest, DocTestSuite, debug, debug_script, debug_src,
    difflib, inspect, linecache, master, namedtuple, pdb, re, register_optionflag,
    run_docstring_examples, script_from_examples, set_unittest_reportflags, tempfile,
    testfile, testmod, testsource, traceback, unittest, warnings]
  downhill: [adaptive, base, build, dataset, first_order, minimize, util]
  dummy_threading: [BoundedSemaphore, Condition, Event, Lock, RLock, Semaphore, Timer,
    activeCount, active_count, currentThread, current_thread, enumerate, setprofile,
    settrace, stack_size, threading]
  email: [Charset, Encoders, Errors, FeedParser, Generator, Header, Iterators, MIMEAudio,
    MIMEBase, MIMEImage, MIMEMessage, MIMEMultipart, MIMENonMultipart, MIMEText, Message,
    Parser, Utils, base64MIME, base64mime, charset, email, encoders, errors, feedparser,
    header, importer, iterators, message, message_from_file, message_from_string,
    mime, parser, quopriMIME, quoprimime, utils]
  enchant: [dict_exists, errors, get_default_language, get_enchant_version, get_param,
    list_dicts, list_languages, pypwl, request_dict, request_pwl_dict, set_param,
    utils, warnings]
  encodings: [aliases, ascii, codecs, normalize_encoding, search_function, utf_8]
  english_parser: [Math, NLs, Nil, action, action_if, action_n_times, action_once,
    action_or_block, action_or_expression, addLongname, adjective, adjectives, adjust_interpret,
    adjust_rollback, adverb, adverbs, algebra, all_methods, all_prepositions, all_quantifiers,
    allow_rollback, angle, any, any_quantifiers, any_ruby_line, app_path, applescript,
    apply_op, arguments, article, articles, as_adverb_as, as_long_condition_block,
    assert_equals, assert_that, assure_same_type, assure_same_type_overwrite, ast,
    ast_operator, attribute, attributes, auxiliary_verbs, bash_action, bash_commands,
    be, be_words, beep, bla, bla_words, block, boolean, boolean_words, bracelet, breaks,
    builtin_method, builtin_methods, c_method, c_methods, call_arg, call_cast, caller,
    caller_depth, caller_name, checkEndOfFile, checkEndOfLine, checkNewline, check_comment,
    check_condition, check_end_of_statement, check_list_condition, check_rollback_allowed,
    classConstDefined, class_constant, class_words, classes, close_bracket, close_tag,
    collection, comment_block, comparation, comparative, compareNode, comparison,
    comparison_words, complex, condition, condition_tree, conjunctions, const_defined,
    const_words, constant, constants, constructor, context, context_keywords, core_methods,
    correlative_conjunctions, current_context, current_expression, current_line, current_node,
    current_offset, current_token, current_type, current_value, current_word, datetime,
    debug, declaration, depth, dictionary_path, did_interpret, do_call_function, do_cast,
    do_compare, do_evaluate, do_evaluate_property, do_execute_block, do_get_class_constant,
    do_interpret, do_math, do_send, doassert, done, done_words, dont_interpret, either_or,
    element_in, emit, emitters, endNode, endNode_, endNoun, end_block, end_expression,
    end_of_statement, english_operators, english_to_math, english_tokens, error_position,
    escape_token, eval_ast, eval_keywords, eval_string, evaluate, evaluate_index,
    evaluate_property, event_kinds, exceptions, execute, expression, expression_or_block,
    extension, extensionMap, extensions, extern_method_call, 'false', false_words,
    fileName, fillers, filter, filter_backtrace, findMethod, flatten, flow_keywords,
    fold_algebra, for_i_in_collection, forever, fraction, functionalselector, future_event,
    gerund, gerundium, get_class, get_module, get_obj, get_type, grep, has_args, has_object,
    html_block, if_then, if_then_else, if_words, ignore_rest_of_line, immediate_json_hash,
    import_keywords, import_module, imports, in_args, in_condition, in_params, in_pipe,
    includes, increase, info, init, inside_list, inspect, instance, integer, interjections,
    interpret, interpret_border, interpretation, interpreting, invalidate_obsolete,
    invoke_keywords, isType, isUnary, is_a, is_comparator, is_dir, is_file, is_math,
    is_method, is_number, is_object_method, isbound, isnumeric, it, javascript, javascript_block,
    javascript_require, jeannie, json_hash, kast, kast_operator_map, kast_operator_map_min,
    keywords, last_pattern, last_result, lastmaybe, let_words, line_number, lines,
    linuxPath, liste, listeners, listselector, load_module_methods, logic_operators,
    long_prepositions, look_ahead, looped_action, looped_action_until, loops, loveHateTo,
    ls, mapType, match_path, max_depth, maybe, maybe_algebra, maybe_cast, maybe_newline,
    maybe_param, maybe_token, maybe_tokens, methodToModulesMap, method_call, method_definition,
    method_dir, method_names, method_tokens, methods, minusMinus, modifier, modifiers,
    moduleClasses, moduleMethods, moduleNames, more_comparative, must_contain, must_contain_before,
    must_contain_before_old, must_contain_substring, must_not_start_with, n_times_action,
    negated, new, newline, newline_tokens, newlines, next_token, nill, nill_words,
    no_keyword, no_keyword_except, no_quantifiers, no_rollback_depth, nod, nodes,
    nonzero, nonzero_keywords, noun, nouns, nth_item, null_comparative, number, number_or_word,
    number_word, numbers, once, once_trigger, once_words, one_or_more, operator, operators,
    original_string, otherKeywords, other_verbs, otherwise, package, package_version,
    pair_prepositions, param, params, parent_node, parse, parse_tokens, parser, piped_actions,
    plusEqual, plusPlus, pointer, pointer_string, possessive_pronouns, postjective,
    postoperations, postpositions, power_parser, prepare_named_args, preposition,
    prepositions, previous_word, print_function, print_pointer, print_variables, pronoun,
    pronouns, property, puts, quantifiers, question_words, quick_expression, quote,
    raiseEnd, raiseNewline, raiseSyntaxError, raise_not_matching, ranger, re, read_block,
    read_source, real, reductions, regexp, regular_json_hash, remove_from_list, remove_tokens,
    repeat_action_while, repeat_every_times, repeat_n_times, repeat_with, require_types,
    resolve, resolve_netbase, rest_of_line, rest_of_statement, result, result_words,
    returns, rollback, rollback_depths, rooty, ruby_action, ruby_block, say, selectable,
    selector, selfModify, self_modifying, self_modifying_operators, set_context, set_token,
    setter, should_not_contain, should_not_start_with, shutil, simpleProperty, special_blocks,
    special_chars, special_verbs, spo, star, start_block, start_block_words, start_shell,
    starts_with, starts_with_, startup, statement, subProperty, svg, swift_hash, system_verbs,
    than_comparative, that, that_are, that_do, that_object_predicate, the, the_, the_noun_that,
    throwing, time_words, to_source, todo, token, token_map, token_number, token_old,
    tokenize, tokens, tokens_, tokens_old, tokenstream, traceback, tree, 'true', true_method,
    true_param, true_variable, true_words, typeName, typeNameMapped, type_keywords,
    type_names, until_loop, untokenize, use_tree, use_wordnet, value, variable, variableTypes,
    variableValues, variables, verb, verb_comparison, verb_node, verbium, verbose,
    verbs, very_verbose, warn, where, while_loop, whose, word, word_regex, wordnet_is_adverb]
  english_tokens: [Nil, adverbs, all_prepositions, all_quantifiers, angle, any_quantifiers,
    articles, ast, attributes, auxiliary_verbs, bash_commands, be_words, bla_words,
    boolean_words, class_words, comparison_words, conjunctions, const_words, constants,
    context_keywords, correlative_conjunctions, done_words, english_operators, eval_keywords,
    event_kinds, extensions, 'false', false_words, fillers, flow_keywords, if_words,
    import_keywords, interjections, invoke_keywords, kast, kast_operator_map, kast_operator_map_min,
    keywords, let_words, logic_operators, long_prepositions, method_tokens, modifiers,
    newline_tokens, nill_words, no_quantifiers, nonzero_keywords, numbers, once_words,
    operators, otherKeywords, other_verbs, pair_prepositions, possessive_pronouns,
    postpositions, prepositions, print_function, pronouns, quantifiers, question_words,
    reductions, require_types, result_words, self_modifying_operators, special_chars,
    special_verbs, start_block_words, system_verbs, time_words, tokenize, 'true',
    true_words, type_keywords, type_names, untokenize]
  ensurepip: [bootstrap, pkgutil, print_function, shutil, ssl, tempfile]
  enum: [pyver, unique]
  errno: [errorcode]
  events: [english_parser, sched, time]
  execnet: [PopenGateway, SocketGateway, SshGateway, default_group, deprecated, dumps,
    gateway, gateway_base, gateway_bootstrap, gateway_io, loads, makegateway, multi,
    rsync, rsync_remote, threadpool, xspec]
  extensions: [Math, beep, exceptions, extension, grep, increase, is_dir, is_file,
    ls, match_path, puts, re, say, shutil]
  ffruit: [sum]
  fileinput: [filelineno, filename, fileno, hook_compressed, hook_encoded, input,
    isfirstline, isstdin, lineno, nextfile]
  flask: [after_this_request, app, appcontext_popped, appcontext_pushed, appcontext_tearing_down,
    blueprints, config, copy_current_request_context, ctx, current_app, debughelpers,
    escape, flash, g, get_flashed_messages, get_template_attribute, globals, got_request_exception,
    has_app_context, has_request_context, helpers, json, json_available, jsonify,
    make_response, message_flashed, module, redirect, render_template, render_template_string,
    request, request_finished, request_started, request_tearing_down, safe_join, send_file,
    send_from_directory, session, sessions, signals, signals_available, stream_with_context,
    template_rendered, templating, url_for, wrappers]
  forbiddenfruit: [ctypes, curse, curses, patchable_builtin, reverse, wraps]
  formatter: [test]
  fortune: [get_random_fortune, main, make_fortune_data_file, pickle, random, random_int]
  ftplib: [all_errors, ftpcp, parse150, parse227, parse229, parse257, print_line,
    socket, ssl, test]
  funcsigs: [absolute_import, division, formatannotation, functools, itertools, print_function,
    re, signature]
  functools: [cmp_to_key, reduce, total_ordering, update_wrapper, wraps]
  functools32: [cmp_to_key, functools32, lru_cache, reduce, reprlib32, total_ordering,
    update_wrapper, wraps]
  gc: [collect, disable, enable, garbage, get_count, get_debug, get_objects, get_referents,
    get_referrers, get_threshold, is_tracked, isenabled, set_debug, set_threshold]
  gensim: [corpora, interfaces, logger, logging, matutils, models, similarities, utils]
  getopt: [do_longs, do_shorts, getopt, gnu_getopt, long_has_args, short_has_arg]
  getpass: [fallback_getpass, getpass, getuser, termios, unix_getpass, warnings, win_getpass]
  gettext: [Catalog, bind_textdomain_codeset, bindtextdomain, c2py, copy, dgettext,
    dngettext, find, gettext, install, ldgettext, ldngettext, lgettext, lngettext,
    locale, ngettext, re, struct, test, textdomain, translation]
  gevent: [absolute_import, get_hub, getcurrent, greenlet, hub, idle, iwait, joinall,
    killall, reinit, sleep, spawn, spawn_later, spawn_raw, timeout, version_info,
    with_timeout]
  glob: [fnmatch, glob, glob0, glob1, has_magic, iglob, magic_check, re]
  glumpy: [color, colormap, figure, graphics, image, imshow, numpy, show, trackball,
    window]
  greenlet: [getcurrent, gettrace, settrace]
  grizzled: [author, cmdline, email, license, title, url]
  grp: [getgrall, getgrgid, getgrnam]
  gunicorn: [v, version_info]
  gzip: [io, read32, struct, time, write32u, zlib]
  hashlib: [algorithms, algorithms_available, algorithms_guaranteed, md5, new, pbkdf2_hmac,
    sha1, sha224, sha256, sha384, sha512]
  heapq: [cmp_lt, heapify, heappop, heappush, heappushpop, heapreplace, merge, nlargest,
    nsmallest]
  hmac: [compare_digest, digest_size, new, trans_36, trans_5C, x]
  humanfriendly: [concatenate, disk_size_units, format_path, format_size, format_timespan,
    interactive_prompt, math, parse_date, parse_size, pluralize, prompt_for_choice,
    re, round_number, time, time_units]
  imaplib: [AllowedVersions, Commands, Continuation, Debug, Flags, Int2AP, InternalDate,
    Internaldate2tuple, Literal, MapCRLF, Mon2num, ParseFlags, Response_code, Time2Internaldate,
    Untagged_response, Untagged_status, binascii, random, re, socket, ssl, subprocess,
    time]
  imghdr: [test, test_bmp, test_exif, test_gif, test_jpeg, test_pbm, test_pgm, test_png,
    test_ppm, test_rast, test_rgb, test_tiff, test_xbm, testall, tests, what]
  imp: [acquire_lock, find_module, get_frozen_object, get_magic, get_suffixes, init_builtin,
    init_frozen, is_builtin, is_frozen, load_compiled, load_dynamic, load_module,
    load_package, load_source, lock_held, new_module, release_lock, reload]
  inspect: [classify_class_attrs, cleandoc, currentframe, dis, findsource, formatargspec,
    formatargvalues, getabsfile, getargs, getargspec, getargvalues, getblock, getcallargs,
    getclasstree, getcomments, getdoc, getfile, getframeinfo, getinnerframes, getlineno,
    getmembers, getmodule, getmoduleinfo, getmodulename, getmro, getouterframes, getsource,
    getsourcefile, getsourcelines, imp, indentsize, isabstract, isbuiltin, isclass,
    iscode, isdatadescriptor, isframe, isfunction, isgenerator, isgeneratorfunction,
    isgetsetdescriptor, ismemberdescriptor, ismethod, ismethoddescriptor, ismodule,
    isroutine, istraceback, joinseq, linecache, modulesbyfile, namedtuple, re, stack,
    strseq, tokenize, trace, walktree]
  interpretation: [add_trigger, events, kast, self_modify, substitute_variables, the]
  interpy: [codec]
  interpy.codec: [register, tokenizer, utils]
  interpy.codec.register: [encodings, interpy_tokenize, interpy_transform, interpy_transform_string,
    interpy_untokenize, with_statement]
  interpy.codec.tokenizer: [inject_tokens]
  interpy.codec.utils: [next, six]
  io: [OpenWrapper, abc]
  ipaddress: [bit_length, collapse_addresses, get_mixed_type_key, int_from_bytes,
    int_to_bytes, ip_address, ip_interface, ip_network, isinstance, math, summarize_address_range,
    v4_int_to_packed, v6_int_to_packed]
  itertools: [tee]
  itsdangerous: [base64, base64_decode, base64_encode, bytes_to_int, compact_json,
    constant_time_compare, hashlib, hmac, int_to_byte, int_to_bytes, is_text_serializer,
    json, number_types, operator, time, want_bytes, zlib]
  jedi: [api, cache, common, debug, defined_names, evaluate, parser, preload_module,
    set_debug_function, settings]
  jinja2: [bccache, clear_caches, compiler, contextfilter, contextfunction, defaults,
    environment, environmentfilter, environmentfunction, escape, evalcontextfilter,
    evalcontextfunction, exceptions, filters, is_undefined, lexer, loaders, nodes,
    optimizer, parser, runtime, tests, utils, visitor]
  jmespath: [ast, compat, compile, exceptions, lexer, parser, search]
  joblib: [cpu_count, delayed, disk, dump, format_stack, func_inspect, hash, hashing,
    load, logger, memory, my_exceptions, numpy_pickle, parallel, pool]
  json: [decoder, dump, dumps, encoder, load, loads, scanner]
  jsonschema: [compat, draft3_format_checker, draft4_format_checker, exceptions, validate,
    validators]
  keyword: [iskeyword, kwlist, main]
  leveldb: [DestroyDB, RepairDB]
  linecache: [cache, checkcache, clearcache, getline, getlines, updatecache]
  locale: [atof, atoi, currency, encodings, format, format_string, functools, getdefaultlocale,
    getlocale, getpreferredencoding, locale_alias, locale_encoding_alias, localeconv,
    nl_langinfo, normalize, operator, re, resetlocale, setlocale, str, strcoll, strxfrm,
    windows_locale]
  lockfile: [LinkFileLock, MkdirFileLock, SQLiteFileLock, absolute_import, functools,
    linklockfile, locked, socket, threading, time, urllib, warnings]
  logging: [addLevelName, atexit, basicConfig, cStringIO, captureWarnings, codecs,
    collections, config, critical, currentframe, debug, disable, exception, fatal,
    getLevelName, getLogger, getLoggerClass, handlers, info, log, logMultiprocessing,
    logProcesses, logThreads, makeLogRecord, raiseExceptions, root, setLoggerClass,
    shutdown, thread, threading, time, traceback, warn, warning, warnings, weakref]
  lxml: [get_include]
  macholib: [pkg_resources, print_function]
  macpath: [abspath, basename, commonprefix, dirname, exists, expanduser, expandvars,
    genericpath, getatime, getctime, getmtime, getsize, isabs, isdir, isfile, islink,
    ismount, join, lexists, normcase, normpath, realpath, split, splitdrive, splitext,
    supports_unicode_filenames, warnings]
  macurl2path: [pathname2url, url2pathname, urllib]
  mailbox: [StringIO, calendar, copy, email, fcntl, rfc822, socket, time, warnings]
  mailcap: [findmatch, findparam, getcaps, listmailcapfiles, lookup, parsefield, parseline,
    readmailcapfile, show, subst, test]
  mako: [ast, cache, codegen, compat, exceptions, ext, filters, lexer, parsetree,
    pygen, pyparser, runtime, template, util]
  markupsafe: [escape, escape_silent, int_types, iteritems, re, soft_unicode, string_types,
    unichr]
  marshal: [dump, dumps, load, loads]
  math: [acos, acosh, asin, asinh, atan, atan2, atanh, ceil, copysign, cos, cosh,
    degrees, e, erf, erfc, exp, expm1, fabs, factorial, floor, fmod, frexp, fsum,
    gamma, hypot, isinf, isnan, ldexp, lgamma, log, log10, log1p, modf, pi, pow, radians,
    sin, sinh, sqrt, tan, tanh, trunc]
  matplotlib: [absolute_import, bad_pyparsing, cbook, checkdep_dvipng, checkdep_ghostscript,
    checkdep_inkscape, checkdep_pdftops, checkdep_ps_distiller, checkdep_tex, checkdep_usetex,
    checkdep_xmllint, colors, compare_versions, compat, contextlib, converter, dateutil,
    default, defaultParams, default_test_modules, distutils, division, f, fontconfig_pattern,
    get_backend, get_cachedir, get_configdir, get_data_path, get_example_data, get_home,
    get_py2exe_datafiles, interactive, is_interactive, is_string_like, is_url, key,
    matplotlib_fname, minor1, minor2, numpy, print_function, pyparsing, rc, rcParams,
    rcParamsDefault, rcParamsOrig, rc_file, rc_file_defaults, rc_params, rc_params_from_file,
    rcdefaults, rcsetup, re, s, six, subprocess, tempfile, test, tk_window_focus,
    tmp, unicode_literals, urlopen, use, validate_backend, verbose, warnings]
  mftracker: [bb, calculateBBCenter, copy, cv, cv2, euclideanDistance, fbtrack, getBB,
    getBBHeight, getBBWidth, getFilledBBPoints, getMedian, getMedianUnmanaged, getRectFromBB,
    lk, lktrack, math, median, normCrossCorrelation, np, predictBB, spsd, time]
  mimetypes: [add_type, common_types, encodings_map, guess_all_extensions, guess_extension,
    guess_type, init, inited, knownfiles, posixpath, read_mime_types, suffix_map,
    types_map, urllib]
  mock: [BaseExceptions, ClassTypes, DescriptorTypes, FunctionAttributes, FunctionTypes,
    builtin, create_autospec, extra, file_spec, inPy3k, inplace, inspect, keyword,
    magic_methods, mock_open, numerics, original_wraps, patch, pprint, re, regex,
    right, self, sentinel, wraps]
  modulefinder: [AddPackagePath, ReplacePackage, dis, generators, imp, marshal, packagePathMap,
    replacePackageMap, struct, test]
  modulegraph: [pkg_resources]
  multiprocessing: [Array, BoundedSemaphore, Condition, Event, JoinableQueue, Lock,
    Manager, Pipe, Pool, Queue, RLock, RawArray, RawValue, Semaphore, Value, active_children,
    allow_connection_pickling, cpu_count, current_process, dummy, forking, freeze_support,
    get_logger, log_to_stderr, pool, process, synchronize, util]
  mutagen: [File, mutagen, version_string, warnings]
  netrc: [pwd, shlex]
  networkx: [LCF_graph, absolute_import, adamic_adar_index, adj_matrix, adjacency_matrix,
    adjacency_spectrum, adjlist, algebraic_connectivity, algebraicconnectivity, algorithms,
    all, all_neighbors, all_pairs_dijkstra_path, all_pairs_dijkstra_path_length, all_pairs_node_connectivity,
    all_pairs_shortest_path, all_pairs_shortest_path_length, all_shortest_paths, all_simple_paths,
    ancestors, approximate_current_flow_betweenness_centrality, articulation_points,
    assortativity, astar, astar_path, astar_path_length, attr_matrix, attr_sparse_matrix,
    attracting, attracting_component_subgraphs, attracting_components, attribute_assortativity_coefficient,
    attribute_mixing_dict, attribute_mixing_matrix, attrmatrix, authority_matrix,
    average_clustering, average_degree_connectivity, average_neighbor_degree, average_node_connectivity,
    average_shortest_path_length, balanced_tree, barabasi_albert_graph, barbell_graph,
    bellman_ford, betweenness, betweenness_centrality, betweenness_centrality_source,
    betweenness_centrality_subset, betweenness_subset, bfs_edges, bfs_predecessors,
    bfs_successors, bfs_tree, biconnected, biconnected_component_edges, biconnected_component_subgraphs,
    biconnected_components, bidirectional_dijkstra, bidirectional_shortest_path, binary,
    binomial_graph, bipartite, bipartite_alternating_havel_hakimi_graph, bipartite_configuration_model,
    bipartite_gnmk_random_graph, bipartite_havel_hakimi_graph, bipartite_preferential_attachment_graph,
    bipartite_random_graph, bipartite_reverse_havel_hakimi_graph, block, blockmodel,
    boundary, breadth_first_search, bull_graph, capacity_scaling, cartesian_product,
    caveman_graph, center, centrality, chordal, chordal_alg, chordal_graph_cliques,
    chordal_graph_treewidth, chvatal_graph, circular_ladder_graph, circular_layout,
    classes, classic, clique, cliques_containing_node, closeness, closeness_centrality,
    closeness_vitality, cluster, clustering, cn_soundarajan_hopcroft, common_neighbors,
    communicability, communicability_alg, communicability_betweenness_centrality,
    communicability_centrality, communicability_centrality_exp, communicability_exp,
    community, complement, complete_bipartite_graph, complete_graph, components, compose,
    compose_all, condensation, configuration_model, connected, connected_caveman_graph,
    connected_component_subgraphs, connected_components, connected_double_edge_swap,
    connected_watts_strogatz_graph, connectivity, convert, convert_matrix, convert_node_labels_to_integers,
    copy, core, core_number, correlation, cost_of_flow, could_be_isomorphic, create_empty_copy,
    cubical_graph, current_flow_betweenness, current_flow_betweenness_centrality,
    current_flow_betweenness_centrality_subset, current_flow_betweenness_subset, current_flow_closeness,
    current_flow_closeness_centrality, cycle_basis, cycle_graph, cycles, dag, davis_southern_women_graph,
    degree, degree_alg, degree_assortativity_coefficient, degree_centrality, degree_histogram,
    degree_mixing_dict, degree_mixing_matrix, degree_pearson_correlation_coefficient,
    degree_seq, degree_sequence_tree, dense, dense_gnm_random_graph, density, depth_first_search,
    desargues_graph, descendants, dfs_edges, dfs_labeled_edges, dfs_postorder_nodes,
    dfs_predecessors, dfs_preorder_nodes, dfs_successors, dfs_tree, diameter, diamond_graph,
    difference, digraph, dijkstra_path, dijkstra_path_length, dijkstra_predecessor_and_distance,
    directed, directed_configuration_model, directed_havel_hakimi_graph, directed_laplacian_matrix,
    disjoint_union, disjoint_union_all, dispersion, distance_measures, distance_regular,
    dodecahedral_graph, dominating, dominating_set, dorogovtsev_goltsev_mendes_graph,
    double_edge_swap, draw, draw_circular, draw_graphviz, draw_networkx, draw_networkx_edge_labels,
    draw_networkx_edges, draw_networkx_labels, draw_networkx_nodes, draw_random, draw_shell,
    draw_spectral, draw_spring, drawing, eccentricity, edge_betweenness, edge_betweenness_centrality,
    edge_betweenness_centrality_subset, edge_boundary, edge_connectivity, edge_current_flow_betweenness_centrality,
    edge_current_flow_betweenness_centrality_subset, edge_load, edgelist, edges, edges_iter,
    ego, ego_graph, eigenvector, eigenvector_centrality, eigenvector_centrality_numpy,
    empty_graph, erdos_renyi_graph, estrada_index, euler, eulerian_circuit, exception,
    expected_degree_graph, external, fast_could_be_isomorphic, fast_gnp_random_graph,
    faster_could_be_isomorphic, fiedler_vector, find_cliques, find_cliques_recursive,
    find_cores, find_induced_nodes, florentine_families_graph, flow, flow_hierarchy,
    flow_matrix, floyd_warshall, floyd_warshall_numpy, floyd_warshall_predecessor_and_distance,
    freeze, from_agraph, from_dict_of_dicts, from_dict_of_lists, from_edgelist, from_numpy_matrix,
    from_scipy_sparse_matrix, frucht_graph, fruchterman_reingold_layout, full_rary_tree,
    function, gaussian_random_partition_graph, general_random_intersection_graph,
    generate_adjlist, generate_edgelist, generate_gexf, !!python/unicode 'generate_gml',
    generate_graph6, generate_graphml, generate_multiline_adjlist, generate_pajek,
    generate_sparse6, generators, generic, geographical_threshold_graph, geometric,
    get_edge_attributes, get_node_attributes, gexf, global_parameters, gml, gn_graph,
    gnc_graph, gnm_random_graph, gnp_random_graph, gnr_graph, goldberg_radzik, google_matrix,
    gpickle, graph, graph6, graph_clique_number, graph_number_of_cliques, graphical,
    graphmatrix, graphml, graphviz_layout, grid_2d_graph, grid_graph, has_path, havel_hakimi_graph,
    heawood_graph, hierarchy, hits, hits_alg, hits_numpy, hits_scipy, house_graph,
    house_x_graph, hub_matrix, hybrid, hypercube_graph, icosahedral_graph, in_degree_centrality,
    incidence_matrix, info, information_centrality, intersection, intersection_all,
    intersection_array, is_aperiodic, is_arborescence, is_attracting_component, is_biconnected,
    is_bipartite, is_branching, is_chordal, is_connected, is_digraphical, is_directed,
    is_directed_acyclic_graph, is_distance_regular, is_dominating_set, is_eulerian,
    is_forest, is_frozen, is_graphical, is_isolate, is_isomorphic, is_kl_connected,
    is_multigraphical, is_pseudographical, is_semiconnected, is_strongly_connected,
    is_tree, is_valid_degree_sequence, is_valid_degree_sequence_erdos_gallai, is_valid_degree_sequence_havel_hakimi,
    is_weakly_connected, isolate, isolates, isomorphism, jaccard_coefficient, k_clique_communities,
    k_core, k_corona, k_crust, k_nearest_neighbors, k_random_intersection_graph, k_shell,
    karate_club_graph, katz, katz_centrality, katz_centrality_numpy, kclique, kl_connected_subgraph,
    kosaraju_strongly_connected_components, krackhardt_kite_graph, kruskal_mst, ladder_graph,
    laplacian_matrix, laplacian_spectrum, laplacianmatrix, layout, leda, lexicographic_product,
    linalg, line, line_graph, link_analysis, link_prediction, load, load_centrality,
    lollipop_graph, make_clique_bipartite, make_max_clique_graph, make_small_graph,
    matching, max_flow_min_cost, max_weight_matching, maximal_independent_set, maximal_matching,
    maximum_flow, maximum_flow_value, min_cost_flow, min_cost_flow_cost, minimum_cut,
    minimum_cut_value, minimum_edge_cut, minimum_node_cut, minimum_spanning_edges,
    minimum_spanning_tree, mis, mixing, mixing_dict, moebius_kantor_graph, mst, multidigraph,
    multigraph, multiline_adjlist, navigable_small_world_graph, negative_edge_cycle,
    neighbor_degree, neighbors, network_simplex, networkx, newman_watts_strogatz_graph,
    node_attribute_xy, node_boundary, node_clique_number, node_connected_component,
    node_connectivity, node_degree_xy, nodes, nodes_iter, non_edges, non_neighbors,
    normalized_laplacian_matrix, null_graph, number_attracting_components, number_connected_components,
    number_of_cliques, number_of_edges, number_of_nodes, number_strongly_connected_components,
    number_weakly_connected_components, numeric_assortativity_coefficient, numeric_mixing_matrix,
    nx, nx_agraph, nx_pylab, nx_shp, nx_yaml, octahedral_graph, operators, out_degree_centrality,
    pagerank, pagerank_alg, pagerank_numpy, pagerank_scipy, pairs, pajek, pappus_graph,
    parse_adjlist, parse_edgelist, !!python/unicode 'parse_gml', parse_graph6, parse_graphml,
    parse_leda, parse_multiline_adjlist, parse_pajek, parse_sparse6, path_graph, periphery,
    petersen_graph, planted_partition_graph, powerlaw_cluster_graph, predecessor,
    preferential_attachment, prim_mst, prim_mst_edges, product, project, project_down,
    project_up, projected_graph, pygraphviz_layout, ra_index_soundarajan_hopcroft,
    radius, random_clustered, random_clustered_graph, random_degree_sequence_graph,
    random_geometric_graph, random_graphs, random_layout, random_lobster, random_partition_graph,
    random_powerlaw_tree, random_powerlaw_tree_sequence, random_regular_graph, random_shell_graph,
    read_adjlist, read_dot, read_edgelist, read_gexf, !!python/unicode 'read_gml',
    read_gpickle, read_graph6, read_graphml, read_leda, read_multiline_adjlist, read_pajek,
    read_shp, read_sparse6, read_weighted_edgelist, read_yaml, readwrite, recognition,
    recursive_simple_cycles, relabel, relabel_gexf_graph, relabel_nodes, relaxed_caveman_graph,
    release, resource_allocation_index, reverse, rich_club_coefficient, richclub,
    s_metric, scale_free_graph, sedgewick_maze_graph, semiconnected, set_edge_attributes,
    set_node_attributes, shell_layout, shortest_path, shortest_path_length, shortest_paths,
    simple_cycles, simple_paths, single_source_dijkstra, single_source_dijkstra_path,
    single_source_dijkstra_path_length, single_source_shortest_path, single_source_shortest_path_length,
    small, smetric, social, sparse6, spectral_layout, spectral_ordering, spectrum,
    spring_layout, square_clustering, star_graph, stochastic, stochastic_graph, stoer_wagner,
    strong_product, strongly_connected, strongly_connected_component_subgraphs, strongly_connected_components,
    strongly_connected_components_recursive, subgraph, swap, symmetric_difference,
    tensor_product, test, tests, tetrahedral_graph, threshold, to_agraph, to_dict_of_dicts,
    to_dict_of_lists, to_edgelist, to_networkx_graph, to_numpy_matrix, to_numpy_recarray,
    to_scipy_sparse_matrix, topological_sort, topological_sort_recursive, transitivity,
    traversal, tree, triangles, trivial_graph, truncated_cube_graph, truncated_tetrahedron_graph,
    tutte_graph, unary, uniform_random_intersection_graph, union, union_all, unweighted,
    utils, view_pygraphviz, vitality, watts_strogatz_graph, waxman_graph, weakly_connected,
    weakly_connected_component_subgraphs, weakly_connected_components, weighted, wheel_graph,
    within_inter_cluster, write_adjlist, write_dot, write_edgelist, write_gexf, !!python/unicode 'write_gml',
    write_gpickle, write_graph6, write_graphml, write_multiline_adjlist, write_pajek,
    write_shp, write_sparse6, write_weighted_edgelist, write_yaml]
  nis: [cat, get_default_domain, maps, match]
  nntplib: [re, socket]
  nodes: [adjectives, ast, builtin_methods, c_methods, classes, context, core_methods,
    current_expression, current_line, current_node, current_offset, current_token,
    current_type, current_value, current_word, debug, depth, did_interpret, emit,
    extensionMap, extensions, in_args, in_condition, in_params, in_pipe, interpret,
    interpret_border, is_number, javascript, kast, last_pattern, last_result, line_number,
    lines, listeners, max_depth, methodToModulesMap, method_names, methods, moduleClasses,
    moduleMethods, moduleNames, negated, no_rollback_depth, nodes, nouns, original_string,
    params, parent_node, parser, previous_word, result, rollback, rollback_depths,
    svg, throwing, token_map, token_number, tokenstream, tree, use_tree, use_wordnet,
    variableTypes, variableValues, variables, verbs, very_verbose]
  nose: [case, collector, config, core, exc, failure, importer, inspector, loader,
    plugins, proxy, pyversion, result, run, runmodule, selector, suite, tools, util,
    with_setup]
  ntpath: [abspath, basename, commonprefix, dirname, exists, expanduser, expandvars,
    genericpath, getatime, getctime, getmtime, getsize, isabs, isdir, isfile, islink,
    ismount, join, lexists, normcase, normpath, realpath, relpath, split, splitdrive,
    splitext, splitunc, supports_unicode_filenames, warnings]
  nturl2path: [pathname2url, url2pathname]
  numbers: [abstractmethod, division]
  numpy: [False_, Inf, Infinity, NaN, ScalarType, True_, abs, absolute, absolute_import,
    add, add_docstring, add_newdoc, add_newdoc_ufunc, add_newdocs, alen, all, allclose,
    alltrue, alterdot, amax, amin, angle, any, append, apply_along_axis, apply_over_axes,
    arange, arccos, arccosh, arcsin, arcsinh, arctan, arctan2, arctanh, argmax, argmin,
    argpartition, argsort, argwhere, around, array, array2string, array_equal, array_equiv,
    array_repr, array_split, array_str, asanyarray, asarray, asarray_chkfinite, ascontiguousarray,
    asfarray, asfortranarray, asmatrix, asscalar, atleast_1d, atleast_2d, atleast_3d,
    average, bartlett, base_repr, bench, binary_repr, bincount, bitwise_and, bitwise_not,
    bitwise_or, bitwise_xor, blackman, bmat, broadcast_arrays, busday_count, busday_offset,
    byte_bounds, c_, can_cast, cast, ceil, char, choose, clip, column_stack, common_type,
    compare_chararrays, compat, compress, concatenate, conj, conjugate, convolve,
    copy, copysign, copyto, core, corrcoef, correlate, cos, cosh, count_nonzero, cov,
    cross, ctypeslib, cumprod, cumproduct, cumsum, datetime_as_string, datetime_data,
    deg2rad, degrees, delete, deprecate, deprecate_with_doc, diag, diag_indices, diag_indices_from,
    diagflat, diagonal, diff, digitize, disp, distutils, divide, division, dot, dsplit,
    dstack, dual, e, ediff1d, einsum, emath, empty, empty_like, equal, euler_gamma,
    exp, exp2, expand_dims, expm1, extract, eye, fabs, fastCopyAndTranspose, fft,
    fill_diagonal, find_common_type, fix, flatnonzero, fliplr, flipud, floor, floor_divide,
    fmax, fmin, fmod, frexp, frombuffer, fromfile, fromfunction, fromiter, frompyfunc,
    fromregex, fromstring, full, full_like, fv, genfromtxt, get_array_wrap, get_include,
    get_printoptions, getbuffer, getbufsize, geterr, geterrcall, geterrobj, gradient,
    greater, greater_equal, hamming, hanning, histogram, histogram2d, histogramdd,
    hsplit, hstack, hypot, i0, identity, imag, in1d, index_exp, indices, inf, info,
    infty, inner, insert, int_asbuffer, interp, intersect1d, invert, ipmt, irr, is_busday,
    isclose, iscomplex, iscomplexobj, isfinite, isfortran, isinf, isnan, isneginf,
    isposinf, isreal, isrealobj, isscalar, issctype, issubclass_, issubdtype, issubsctype,
    iterable, ix_, kaiser, kron, ldexp, left_shift, less, less_equal, lexsort, lib,
    linalg, linspace, little_endian, load, loads, loadtxt, log, log10, log1p, log2,
    logaddexp, logaddexp2, logical_and, logical_not, logical_or, logical_xor, logspace,
    lookfor, ma, mafromtxt, mask_indices, mat, math, matrixlib, max, maximum, maximum_sctype,
    may_share_memory, mean, median, meshgrid, mgrid, min, min_scalar_type, minimum,
    mintypecode, mirr, mod, modf, msort, multiply, nan, nan_to_num, nanargmax, nanargmin,
    nanmax, nanmean, nanmedian, nanmin, nanpercentile, nanstd, nansum, nanvar, nbytes,
    ndfromtxt, ndim, negative, nested_iters, newaxis, newbuffer, nextafter, nonzero,
    not_equal, nper, npv, numarray, obj2sctype, ogrid, oldnumeric, ones, ones_like,
    outer, packbits, pad, partition, percentile, pi, piecewise, pkgload, place, pmt,
    poly, polyadd, polyder, polydiv, polyfit, polyint, polymul, polynomial, polysub,
    polyval, power, ppmt, print_function, prod, product, promote_types, ptp, put,
    putmask, pv, r_, rad2deg, radians, random, rank, rate, ravel, ravel_multi_index,
    real, real_if_close, rec, recfromcsv, recfromtxt, reciprocal, remainder, repeat,
    require, reshape, resize, restoredot, result_type, right_shift, rint, roll, rollaxis,
    roots, rot90, round, round_, row_stack, s_, safe_eval, save, savetxt, savez, savez_compressed,
    sctype2char, sctypeDict, sctypeNA, sctypes, searchsorted, select, set_numeric_ops,
    set_printoptions, set_string_function, setbufsize, setdiff1d, seterr, seterrcall,
    seterrobj, setxor1d, shape, show_config, sign, signbit, sin, sinc, sinh, size,
    sometrue, sort, sort_complex, source, spacing, split, sqrt, square, squeeze, std,
    subtract, sum, swapaxes, take, tan, tanh, tensordot, test, testing, tile, trace,
    transpose, trapz, tri, tril, tril_indices, tril_indices_from, trim_zeros, triu,
    triu_indices, triu_indices_from, true_divide, trunc, typeDict, typeNA, typecodes,
    typename, union1d, unique, unpackbits, unravel_index, unwrap, vander, var, vdot,
    vsplit, vstack, warnings, where, who, zeros, zeros_like]
  numpydoc: [absolute_import, division, docscrape, docscrape_sphinx, numpydoc, print_function,
    setup]
  objc: [Accessor, Category, IBAction, IBOutlet, accessor, addConvenienceForBasicMapping,
    addConvenienceForBasicSequence, addConvenienceForClass, allocateBuffer, callbackFor,
    classAddMethod, classAddMethods, context, createOpaquePointerType, createStructAlias,
    createStructType, currentBundle, dyld_find, dyld_framework, dyld_library, getAssociatedObject,
    getClassList, getInstanceVariable, getStrBridgeEnabled, getUseKVOForSetattr, getVerbose,
    infoForFramework, initFrameworkWrapper, instancemethod, listInstanceVariables,
    loadBundle, loadBundleFunctions, loadBundleVariables, loadFunctionList, loadSpecialVar,
    lookUpClass, namedSelector, namedselector, nil, nsdata__getitem__, nsdata__getslice__,
    nsdata__new__, nsdata__str__, nsmutabledata__setitem__, nsmutabledata__setslice__,
    objc, options, parseBridgeSupport, pathForFramework, platform, propertiesForClass,
    protocolNamed, protocolsForClass, protocolsForProcess, pyobjc_id, recycleAutoreleasePool,
    registerABCForClass, registerCFSignature, registerDateType, registerListType,
    registerMappingType, registerMetaDataForSelector, registerSetType, registerStructAlias,
    removeAssociatedObjects, removeAutoreleasePool, repythonify, selectorFor, setAssociatedObject,
    setInstanceVariable, setStrBridgeEnabled, setUseKVOForSetattr, setVerbose, signature,
    splitSignature, splitStructSignature, synthesize, typedAccessor, typedSelector,
    warnings]
  opcode: [cmp_op, hascompare, hasconst, hasfree, hasjabs, hasjrel, haslocal, hasname,
    opmap, opname]
  opencl: [broadcast, cl_mem, cl_types, clgl, context, copencl, copyreg, ctypes, empty,
    empty_image, errors, event, from_host, get_include, get_platforms, gl, int_types,
    kernel, pickle_cl_vector, program, queue, test, type_formats, uint_types]
  operator: [abs, add, and_, concat, contains, countOf, delitem, delslice, div, eq,
    floordiv, ge, getitem, getslice, gt, iadd, iand, iconcat, idiv, ifloordiv, ilshift,
    imod, imul, indexOf, inv, invert, ior, ipow, irepeat, irshift, isCallable, isMappingType,
    isNumberType, isSequenceType, is_, is_not, isub, itruediv, ixor, le, lshift, lt,
    mod, mul, ne, neg, not_, or_, pos, pow, repeat, rshift, sequenceIncludes, setitem,
    setslice, sub, truediv, truth, xor]
  optparse: [check_builtin, check_choice, gettext, isbasestring, textwrap]
  os: [UserDict, abort, access, altsep, chdir, chflags, chmod, chown, chroot, close,
    closerange, confstr, confstr_names, ctermid, curdir, defpath, devnull, dup, dup2,
    environ, errno, execl, execle, execlp, execlpe, execv, execve, execvp, execvpe,
    extsep, fchdir, fchmod, fchown, fdopen, fork, forkpty, fpathconf, fstat, fstatvfs,
    fsync, ftruncate, getcwd, getcwdu, getegid, getenv, geteuid, getgid, getgroups,
    getloadavg, getlogin, getpgid, getpgrp, getpid, getppid, getsid, getuid, initgroups,
    isatty, kill, killpg, lchflags, lchmod, lchown, linesep, link, listdir, lseek,
    lstat, major, makedev, makedirs, minor, mkdir, mkfifo, mknod, name, nice, open,
    openpty, pardir, path, pathconf, pathconf_names, pathsep, pipe, popen, popen2,
    popen3, popen4, putenv, read, readlink, remove, removedirs, rename, renames, rmdir,
    sep, setegid, seteuid, setgid, setgroups, setpgid, setpgrp, setregid, setreuid,
    setsid, setuid, spawnl, spawnle, spawnlp, spawnlpe, spawnv, spawnve, spawnvp,
    spawnvpe, stat, stat_float_times, statvfs, strerror, symlink, sys, sysconf, sysconf_names,
    system, tcgetpgrp, tcsetpgrp, tempnam, times, tmpfile, tmpnam, ttyname, umask,
    uname, unlink, unsetenv, urandom, utime, wait, wait3, wait4, waitpid, walk, write]
  os.path: [abspath, basename, commonprefix, dirname, exists, expanduser, expandvars,
    genericpath, getatime, getctime, getmtime, getsize, isabs, isdir, isfile, islink,
    ismount, join, lexists, normcase, normpath, realpath, relpath, samefile, sameopenfile,
    samestat, split, splitdrive, splitext, supports_unicode_filenames, warnings]
  panda: [base64, canonical_path, canonical_querystring, generate_signature, generate_timestamp,
    hashlib, hmac, httplib, urlescape, urllib]
  pandas: [NaT, algos, bdate_range, compat, computation, concat, core, crosstab, cut,
    date_range, datetools, describe_option, eval, ewma, ewmcorr, ewmcov, ewmstd, ewmvar,
    ewmvol, expanding_apply, expanding_corr, expanding_corr_pairwise, expanding_count,
    expanding_cov, expanding_kurt, expanding_max, expanding_mean, expanding_median,
    expanding_min, expanding_quantile, expanding_skew, expanding_std, expanding_sum,
    expanding_var, factorize, fama_macbeth, get_dummies, get_option, get_store, groupby,
    hashtable, index, infer_freq, info, io, isnull, json, lib, load, lreshape, match,
    melt, merge, msgpack, notnull, np, offsets, ols, options, ordered_merge, pandas,
    parser, period_range, pivot, pivot_table, plot_params, pnow, qcut, read_clipboard,
    read_csv, read_excel, read_fwf, read_hdf, read_html, read_json, read_msgpack,
    read_pickle, read_sql, read_stata, read_table, reset_option, rolling_apply, rolling_corr,
    rolling_corr_pairwise, rolling_count, rolling_cov, rolling_kurt, rolling_max,
    rolling_mean, rolling_median, rolling_min, rolling_quantile, rolling_skew, rolling_std,
    rolling_sum, rolling_var, rolling_window, save, scatter_matrix, set_eng_float_format,
    set_option, sparse, stats, to_datetime, to_msgpack, to_pickle, to_timedelta, tools,
    tseries, tslib, unique, util, value_counts, value_range, wide_to_long]
  paramiko: [GSSAuth, agent, auth_handler, ber, buffered_pipe, channel, client, common,
    compress, config, dsskey, ecdsakey, file, hostkeys, io_sleep, kex_gex, kex_group1,
    kex_group14, kex_gss, message, packet, pkey, primes, proxy, py3compat, resource,
    rsakey, server, sftp, sftp_attr, sftp_client, sftp_file, sftp_handle, sftp_server,
    sftp_si, ssh_exception, ssh_gss, transport, util]
  parser: [ast2list, ast2tuple, compileast, compilest, expr, isexpr, issuite, sequence2ast,
    sequence2st, st2list, st2tuple, suite, tuple2ast, tuple2st]
  pathlib: [contextmanager, fnmatch, functools, intern, io, nt, ntpath, posixpath,
    re, supports_symlinks, time, urlquote, urlquote_from_bytes]
  pdb: [bdb, cmd, find_function, help, line_prefix, linecache, main, pm, post_mortem,
    pprint, re, run, runcall, runctx, runeval, set_trace, test, traceback]
  pickle: [PyStringMap, StringIO, StringTypes, classmap, compatible_formats, decode_long,
    dispatch_table, dump, dumps, encode_long, format_version, load, loads, marshal,
    mloads, re, struct, whichmodule]
  pickletools: [anyobject, code2op, decimalnl_long, decimalnl_short, decode_long,
    dis, float8, floatnl, genops, int4, long1, long4, markobject, opcodes, optimize,
    pybool, pydict, pyfloat, pyint, pyinteger_or_bool, pylist, pylong, pynone, pystring,
    pytuple, pyunicode, read_decimalnl_long, read_decimalnl_short, read_float8, read_floatnl,
    read_int4, read_long1, read_long4, read_string1, read_string4, read_stringnl,
    read_stringnl_noescape, read_stringnl_noescape_pair, read_uint1, read_uint2, read_unicodestring4,
    read_unicodestringnl, stackslice, string1, string4, stringnl, stringnl_noescape,
    stringnl_noescape_pair, uint1, uint2, unicodestring4, unicodestringnl]
  pip: [absolute_import, autocomplete, basecommand, baseparser, bazaar, check_isolated,
    cmdoptions, commands, commands_dict, compat, create_main_parser, deprecation,
    download, exceptions, get_installed_distributions, get_prog, get_similar_commands,
    get_summaries, git, index, locations, logger, logging, main, mercurial, models,
    operations, optparse, parseopts, pep425tags, pip, re, req, status_codes, subversion,
    utils, vcs, warnings, wheel]
  pipes: [makepipeline, quote, re, stepkinds, tempfile]
  pkg_resources: [absolute_import, add_activation_listener, cached_files, cleanup_resources,
    collections, compatible_platforms, darwinVersionString, declare_namespace, email,
    empty_provider, ensure_directory, evaluate_marker, extraction_error, extraction_path,
    file_ns_handler, find_distributions, find_eggs_in_zip, find_nothing, find_on_path,
    fixup_namespace_packages, functools, get_build_platform, get_cache_path, get_default_cache,
    get_distribution, get_entry_info, get_entry_map, get_importer, get_platform, get_provider,
    get_supported_platform, importlib_machinery, invalid_marker, io, isdir, issue_warning,
    iter_entry_points, iteritems, load_entry_point, macosVersionString, normalize_path,
    null_ns_handler, operator, os_open, packaging, parse_requirements, parse_version,
    parser, pkg_resources, pkgutil, platform, plistlib, postprocess, re, register_finder,
    register_loader_type, register_namespace_handler, require, resource_exists, resource_filename,
    resource_isdir, resource_listdir, resource_stream, resource_string, run_main,
    run_script, safe_extra, safe_name, safe_version, set_extraction_path, split, split_sections,
    string_types, symbol, tempfile, textwrap, time, to_filename, token, urlparse,
    urlunparse, warnings, working_set, yield_lines, zipfile, zipimport]
  pkgutil: [extend_path, find_loader, get_data, get_importer, get_loader, imp, iter_importer_modules,
    iter_importers, iter_modules, iter_zipimport_modules, read_code, simplegeneric,
    walk_packages, zipimport]
  plac: [annotations, argparse, default_help, getargspec, import_main, inspect, is_annotation,
    iterable, parser_from, pconf, re, runp, stdout]
  platform: [architecture, dist, java_ver, libc_ver, linux_distribution, mac_ver,
    machine, node, platform, processor, python_branch, python_build, python_compiler,
    python_implementation, python_revision, python_version, python_version_tuple,
    re, release, system_alias, win32_ver]
  plistlib: [StringIO, binascii, datetime, re, readPlist, readPlistFromResource, readPlistFromString,
    warnings, writePlist, writePlistToResource, writePlistToString]
  poplib: [re, socket, ssl]
  poster: [encode, poster, streaminghttp]
  power_parser: [NLs, adjectives, adjust_interpret, adjust_rollback, allow_rollback,
    angle, any, app_path, assert_equals, block, builtin_methods, c_methods, caller,
    caller_depth, caller_name, checkEndOfFile, checkEndOfLine, checkNewline, check_comment,
    check_rollback_allowed, classes, comment_block, complex, context, core_methods,
    current_expression, current_line, current_node, current_offset, current_token,
    current_type, current_value, current_word, debug, depth, dictionary_path, did_interpret,
    do_interpret, doassert, dont_interpret, emit, english_tokens, error_position,
    escape_token, extensionMap, extensions, filter_backtrace, flatten, fraction, ignore_rest_of_line,
    in_args, in_condition, in_params, in_pipe, info, init, integer, interpret, interpret_border,
    interpreting, invalidate_obsolete, is_number, isnumeric, javascript, last_pattern,
    last_result, lastmaybe, line_number, lines, listeners, load_module_methods, look_ahead,
    max_depth, maybe, maybe_newline, maybe_tokens, methodToModulesMap, method_names,
    methods, moduleClasses, moduleMethods, moduleNames, must_contain, must_contain_before,
    must_contain_before_old, negated, newline, newlines, next_token, no_rollback_depth,
    nodes, nouns, number, number_word, one_or_more, original_string, params, parent_node,
    parse, parse_tokens, parser, pointer, pointer_string, previous_word, print_function,
    print_pointer, raiseEnd, raiseNewline, raise_not_matching, re, read_source, real,
    remove_tokens, rest_of_line, rest_of_statement, result, rollback, rollback_depths,
    set_token, star, starts_with, svg, the, throwing, to_source, token, token_map,
    token_number, token_old, tokenize, tokens, tokens_old, tokenstream, tree, use_tree,
    use_wordnet, variableTypes, variableValues, variables, verbose, verbs, very_verbose,
    warn]
  pprint: [isreadable, isrecursive, pformat, pprint, saferepr, warnings]
  profile: [Stats, help, main, marshal, resgetrusage, resource, run, runctx, time]
  pstats: [add_callers, add_func_stats, cmp_to_key, count_calls, f8, func_get_function_name,
    func_std_string, func_strip_path, marshal, re, time]
  psutil: [avail_phymem, avail_virtmem, boot_time, callable, collections, cpu_count,
    cpu_percent, cpu_times, cpu_times_percent, disk_io_counters, disk_partitions,
    disk_usage, functools, get_pid_list, get_process_list, get_users, net_connections,
    net_io_counters, network_io_counters, phymem_usage, pid_exists, pids, process_iter,
    pwd, signal, subprocess, swap_memory, test, time, total_virtmem, used_phymem,
    used_virtmem, users, version_info, virtmem_usage, virtual_memory, wait_procs,
    warnings]
  pty: [master_open, select, slave_open, spawn, tty]
  pwd: [getpwall, getpwnam, getpwuid]
  py: [apipkg, builtin, code, iniconfig, io, log, process, std, test, xml]
  py2app: [pkg_resources, setuptools]
  py_compile: [compile, imp, main, marshal, traceback, wr_long]
  pyasn: [Radix, collapse_addresses, ip_network, pyasn_radix, re]
  pyasn1: [codec, compat, debug, type]
  pyaudio: [get_format_from_width, get_portaudio_version, get_portaudio_version_text,
    get_sample_size, pa, paAL, paALSA, paASIO, paAbort, paBadIODeviceCombination,
    paBadStreamPtr, paBeOS, paBufferTooBig, paBufferTooSmall, paCanNotReadFromACallbackStream,
    paCanNotReadFromAnOutputOnlyStream, paCanNotWriteToACallbackStream, paCanNotWriteToAnInputOnlyStream,
    paComplete, paContinue, paCoreAudio, paCustomFormat, paDeviceUnavailable, paDirectSound,
    paFloat32, paHostApiNotFound, paInDevelopment, paIncompatibleHostApiSpecificStreamInfo,
    paIncompatibleStreamHostApi, paInputOverflow, paInputOverflowed, paInputUnderflow,
    paInsufficientMemory, paInt16, paInt24, paInt32, paInt8, paInternalError, paInvalidChannelCount,
    paInvalidDevice, paInvalidFlag, paInvalidHostApi, paInvalidSampleRate, paJACK,
    paMME, paNoDevice, paNoError, paNotInitialized, paNullCallback, paOSS, paOutputOverflow,
    paOutputUnderflow, paOutputUnderflowed, paPrimingOutput, paSampleFormatNotSupported,
    paSoundManager, paStreamIsNotStopped, paStreamIsStopped, paTimedOut, paUInt8,
    paUnanticipatedHostError, paWASAPI, paWDMKS]
  pyclbr: [imp, readmodule, readmodule_ex, tokenize]
  pydoc: [allmethods, apropos, classify_class_attrs, classname, cli, cram, describe,
    doc, expandtabs, extract_tb, find, getdoc, getpager, gui, help, html, imp, importfile,
    inspect, isdata, ispackage, ispath, join, locale, locate, lower, pager, pathdirs,
    pipepager, pkgutil, plain, plainpager, re, render_doc, replace, resolve, rfind,
    rstrip, safeimport, serve, source_synopsis, split, splitdoc, strip, stripid, synopsis,
    tempfilepager, text, ttypager, visiblename, warnings, writedoc, writedocs]
  pydoc_data: [topics]
  pyexpat: [ErrorString, ParserCreate, errors, expat_CAPI, features, model, native_encoding,
    version_info]
  pygments: [BytesIO, filter, filters, format, formatter, formatters, highlight, lex,
    lexer, lexers, modeline, plugin, regexopt, style, styles, token, unistring, util]
  pygpu: [dtypes, elemwise, get_default_context, gpuarray, reduction, register_dtype,
    set_default_context]
  pylab: [False_, Inf, Infinity, NaN, ScalarType, True_, absolute, absolute_import,
    acorr, add, add_docstring, add_newdoc, add_newdoc_ufunc, add_newdocs, alen, all,
    allclose, alltrue, alterdot, amap, amax, amin, angle, angle_spectrum, annotate,
    any, append, apply_along_axis, apply_over_axes, arange, arccos, arccosh, arcsin,
    arcsinh, arctan, arctan2, arctanh, argmax, argmin, argpartition, argsort, argwhere,
    around, array, array2string, array_equal, array_equiv, array_repr, array_split,
    array_str, arrow, asanyarray, asarray, asarray_chkfinite, ascontiguousarray, asfarray,
    asfortranarray, asmatrix, asscalar, atleast_1d, atleast_2d, atleast_3d, autoscale,
    autumn, average, axes, axhline, axhspan, axis, axvline, axvspan, bar, barbs, barh,
    bartlett, base_repr, bench, beta, binary_repr, bincount, binomial, bitwise_and,
    bitwise_not, bitwise_or, bitwise_xor, bivariate_normal, blackman, bmat, bone,
    box, boxplot, broadcast_arrays, broken_barh, busday_count, busday_offset, byte_bounds,
    c_, can_cast, cast, cbook, ceil, center_matrix, char, chisquare, cholesky, choose,
    cla, clabel, clf, clim, clip, cm, cohere, colorbar, colormaps, colors, column_stack,
    common_type, compare_chararrays, compress, concatenate, cond, conj, conjugate,
    connect, contour, contourf, convolve, cool, copper, copy, copysign, copyto, corrcoef,
    correlate, cos, cosh, count_nonzero, cov, cross, csd, csv2rec, ctypeslib, cumprod,
    cumproduct, cumsum, date2num, datestr2num, datetime, datetime_as_string, datetime_data,
    dedent, deg2rad, degrees, delaxes, delete, demean, deprecate, deprecate_with_doc,
    det, detrend, detrend_linear, detrend_mean, detrend_none, diag, diag_indices,
    diag_indices_from, diagflat, diagonal, diff, digitize, disconnect, disp, dist,
    dist_point_to_segment, distances_along_curve, divide, division, docstring, dot,
    drange, draw, draw_if_interactive, dsplit, dstack, e, ediff1d, eig, eigh, eigvals,
    eigvalsh, einsum, emath, empty, empty_like, entropy, epoch2num, equal, errorbar,
    euler_gamma, eventplot, exception_to_str, exp, exp2, exp_safe, expand_dims, expm1,
    exponential, extract, eye, f, fabs, fastCopyAndTranspose, fft, fft2, fftfreq,
    fftn, fftpack, fftpack_lite, fftshift, fftsurr, figaspect, figimage, figlegend,
    fignum_exists, figtext, figure, fill, fill_between, fill_betweenx, fill_diagonal,
    find, find_common_type, findobj, fix, flag, flatnonzero, flatten, fliplr, flipud,
    floor, floor_divide, fmax, fmin, fmod, frange, frexp, frombuffer, fromfile, fromfunction,
    fromiter, frompyfunc, fromregex, fromstring, full, full_like, fv, gamma, gca,
    gcf, gci, genfromtxt, geometric, get, get_array_wrap, get_backend, get_cmap, get_current_fig_manager,
    get_figlabels, get_fignums, get_include, get_plot_commands, get_printoptions,
    get_scale_docs, get_scale_names, get_sparse_matrix, get_state, get_xyz_where,
    getbuffer, getbufsize, geterr, geterrcall, geterrobj, getp, ginput, gradient,
    gray, greater, greater_equal, grid, griddata, gumbel, hamming, hanning, helper,
    hexbin, hfft, hist, hist2d, histogram, histogram2d, histogramdd, hlines, hold,
    hot, hsplit, hstack, hsv, hypergeometric, hypot, i0, identity, ifft, ifft2, ifftn,
    ifftshift, ihfft, imag, imread, imsave, imshow, in1d, index_exp, indices, inf,
    info, infty, inner, insert, inside_poly, int_asbuffer, interactive, interp, intersect1d,
    inv, invert, ioff, ion, ipmt, irfft, irfft2, irfftn, irr, is_busday, is_closed_polygon,
    is_numlike, is_string_like, isclose, iscomplex, iscomplexobj, isfinite, isfortran,
    ishold, isinf, isinteractive, isnan, isneginf, isposinf, ispower2, isreal, isrealobj,
    isscalar, issctype, issubclass_, issubdtype, issubsctype, isvector, iterable,
    ix_, jet, kaiser, kron, l1norm, l2norm, lapack_lite, laplace, ldexp, left_shift,
    legend, less, less_equal, lexsort, linalg, linspace, little_endian, load, loads,
    loadtxt, locator_params, log, log10, log1p, log2, logaddexp, logaddexp2, logical_and,
    logical_not, logical_or, logical_xor, logistic, loglog, lognormal, logseries,
    logspace, longest_contiguous_ones, longest_ones, lookfor, lstsq, ma, mafromtxt,
    magnitude_spectrum, margins, mask_indices, mat, math, matplotlib, matrix_power,
    matrix_rank, matshow, maximum, maximum_sctype, may_share_memory, mean, median,
    meshgrid, mgrid, min_scalar_type, minimum, minorticks_off, minorticks_on, mintypecode,
    mirr, mlab, mod, modf, movavg, mpl, msort, multinomial, multiply, multivariate_normal,
    mx2num, nan, nan_to_num, nanargmax, nanargmin, nanmax, nanmean, nanmedian, nanmin,
    nanpercentile, nanstd, nansum, nanvar, nbytes, ndfromtxt, ndim, negative, negative_binomial,
    nested_iters, new_figure_manager, newaxis, newbuffer, nextafter, noncentral_chisquare,
    noncentral_f, nonzero, norm, norm_flat, normal, normalize, normpdf, not_equal,
    np, nper, npv, num2date, num2epoch, obj2sctype, ogrid, ones, ones_like, outer,
    over, packbits, pad, pareto, partition, path_length, pause, pcolor, pcolormesh,
    percentile, permutation, phase_spectrum, pi, pie, piecewise, pink, pinv, pkgload,
    place, plot, plot_date, plotfile, plotting, plt, pmt, poisson, polar, poly, poly_below,
    poly_between, polyadd, polyder, polydiv, polyfit, polyint, polymul, polysub, polyval,
    power, ppmt, prctile, prctile_rank, prepca, print_function, prism, prod, product,
    promote_types, psd, ptp, put, putmask, pv, pylab_setup, qr, quiver, quiverkey,
    r_, rad2deg, radians, rand, randint, randn, random, random_integers, random_sample,
    ranf, rank, rate, ravel, ravel_multi_index, rayleigh, rc, rcParams, rcParamsDefault,
    rc_context, rcdefaults, real, real_if_close, rec, rec2csv, rec_append_fields,
    rec_drop_fields, rec_join, recfromcsv, recfromtxt, reciprocal, register_cmap,
    remainder, repeat, require, reshape, resize, restoredot, result_type, rfft, rfft2,
    rfftfreq, rfftn, rgrids, right_shift, rint, rk4, rms_flat, roll, rollaxis, roots,
    rot90, round_, row_stack, s_, safe_eval, sample, save, savefig, savetxt, savez,
    savez_compressed, sca, scatter, sci, sctype2char, sctypeDict, sctypeNA, sctypes,
    searchsorted, seed, segments_intersect, select, semilogx, semilogy, set_cmap,
    set_numeric_ops, set_printoptions, set_state, set_string_function, setbufsize,
    setdiff1d, seterr, seterrcall, seterrobj, setp, setxor1d, shape, show, show_config,
    shuffle, sign, signbit, sin, sinc, sinh, six, size, slogdet, slopes, solve, sometrue,
    sort, sort_complex, source, spacing, specgram, spectral, split, spring, spy, sqrt,
    square, squeeze, stackplot, standard_cauchy, standard_exponential, standard_gamma,
    standard_normal, standard_t, std, stem, step, stineman_interp, streamplot, style,
    subplot, subplot2grid, subplot_tool, subplots, subplots_adjust, subtract, sum,
    summer, suptitle, svd, swapaxes, switch_backend, table, take, tan, tanh, tensordot,
    tensorinv, tensorsolve, test, text, thetagrids, tick_params, ticklabel_format,
    tight_layout, tile, title, trace, transpose, trapz, tri, triangular, tricontour,
    tricontourf, tril, tril_indices, tril_indices_from, trim_zeros, tripcolor, triplot,
    triu, triu_indices, triu_indices_from, true_divide, trunc, twinx, twiny, typeDict,
    typeNA, typecodes, typename, unicode_literals, uniform, union1d, unique, unpackbits,
    unravel_index, unwrap, vander, var, vdot, vector_lengths, violinplot, vlines,
    vonmises, vsplit, vstack, waitforbuttonpress, wald, warnings, weibull, where,
    who, window_hanning, window_none, winter, xcorr, xkcd, xlabel, xlim, xscale, xticks,
    ylabel, ylim, yscale, yticks, zeros, zeros_like, zipf]
  pylint: [run_epylint, run_pylint, run_pylint_gui, run_pyreverse, run_symilar]
  pyopencl: [bitlog2, compiler_output, create_program_with_built_in_kernels, create_some_context,
    enable_debugging, enqueue_barrier, enqueue_copy, enqueue_copy_buffer, enqueue_copy_buffer_rect,
    enqueue_copy_buffer_to_image, enqueue_copy_image, enqueue_copy_image_to_buffer,
    enqueue_fill_buffer, enqueue_fill_image, enqueue_map_buffer, enqueue_map_image,
    enqueue_marker, enqueue_migrate_mem_objects, enqueue_nd_range_kernel, enqueue_read_buffer,
    enqueue_read_buffer_rect, enqueue_read_image, enqueue_task, enqueue_write_buffer,
    enqueue_write_buffer_rect, enqueue_write_image, get_cl_header_version, get_supported_image_formats,
    have_gl, image_from_array, link_program, unload_platform_compiler, wait_for_events]
  pyparsing: [alphanums, alphas, alphas8bit, anyCloseTag, anyOpenTag, cStyleComment,
    col, commaSeparatedList, commonHTMLEntity, countedArray, cppStyleComment, dblQuotedString,
    dblSlashComment, delimitedList, dictOf, downcaseTokens, fname, getTokensEndLoc,
    hexnums, htmlComment, indentedBlock, infixNotation, javaStyleComment, keepOriginalText,
    lineEnd, lineStart, lineno, locatedExpr, makeHTMLTags, makeXMLTags, matchOnlyAtCol,
    matchPreviousExpr, matchPreviousLiteral, nestedExpr, nullDebugAction, nums, oneOf,
    opAssoc, operatorPrecedence, originalTextFor, printables, punc8bit, pythonStyleComment,
    quotedString, removeQuotes, replaceHTMLEntity, replaceWith, restOfLine, sglQuotedString,
    singleArgBuiltins, srange, sre_constants, stringEnd, stringStart, traceParseAction,
    ungroup, unicodeString, upcaseTokens, withAttribute]
  pyquery: [cssselectpatch, openers, pyquery]
  pytools: [FunctionValueCache, a_star, absolute_import, add_python_path_relative_to_script,
    add_tuples, all, all_equal, all_roughly_equal, any, argmax, argmax2, argmin, argmin2,
    assert_not_a_file, average, cartesian_product, cartesian_product_sum, comb, common_dtype,
    common_prefix, decorate, decorator, decorator_module, delta, distinct_pairs, div_ceil,
    division, enumerate_basic_directions, factorial, find_max_where, flatten, general_sum,
    generate_all_integer_tuples, generate_all_integer_tuples_below, generate_all_nonnegative_integer_tuples,
    generate_all_positive_integer_tuples, generate_decreasing_nonnegative_tuples_summing_to,
    generate_nonnegative_integer_tuples_below, generate_nonnegative_integer_tuples_summing_to_at_most,
    generate_permutations, generate_positive_integer_tuples_below, generate_unique_names,
    generate_unique_permutations, generate_unique_possibilities, get_read_from_map_from_permutation,
    get_write_to_map_from_permutation, indices_in_shape, input, intern, invoke_editor,
    is_single_valued, len_iterable, levi_civita, linear_combination, match_precision,
    memoize, memoize_method, memoize_method_nested, memoize_method_with_uncached,
    memoize_on_first_arg, monkeypatch_class, monkeypatch_method, my_decorator, negate_tuple,
    norm_1, norm_2, norm_inf, norm_p, one, operator, partition, partition2, perm,
    print_function, product, reduce, reverse_dictionary, set_sum, shift, single_valued,
    six, std_deviation, string_histogram, to_uncomplex_dtype, typedump, uniform_interval_splitting,
    variance, wandering_element, word_wrap]
  pytz: [FixedOffset, all_timezones, all_timezones_set, ascii, build_tzinfo, common_timezones,
    common_timezones_set, country_names, country_timezones, datetime, exceptions,
    gettext, lazy, open_resource, resource_exists, resource_stream, timezone, tzfile,
    tzinfo, unpickler, utc]
  qtutils: [disconnect_contextmanager, inmain, inmain_decorator, inmain_later, inthread,
    invoke_in_main, locking, print_function, qsettings_wrapper, qtlock]
  quopri: [a2b_qp, b2a_qp, decode, decodestring, encode, encodestring, ishex, main,
    needsquoting, quote, unhex]
  random: [betavariate, choice, division, expovariate, gammavariate, gauss, getrandbits,
    getstate, jumpahead, lognormvariate, normalvariate, paretovariate, randint, random,
    randrange, sample, seed, setstate, shuffle, triangular, uniform, vonmisesvariate,
    weibullvariate]
  re: [compile, copy_reg, escape, findall, finditer, match, purge, search, split,
    sre_compile, sre_parse, sub, subn, template]
  readline: [add_history, clear_history, get_begidx, get_completer, get_completer_delims,
    get_completion_type, get_current_history_length, get_endidx, get_history_item,
    get_history_length, get_line_buffer, insert_text, parse_and_bind, read_history_file,
    read_init_file, redisplay, remove_history_item, replace_history_item, set_completer,
    set_completer_delims, set_completion_display_matches_hook, set_history_length,
    set_pre_input_hook, set_startup_hook, write_history_file]
  requests: [adapters, api, auth, certs, codes, compat, cookies, delete, exceptions,
    get, head, hooks, logging, models, options, packages, patch, post, put, request,
    session, sessions, status_codes, structures, utils]
  resource: [getpagesize, getrlimit, getrusage, setrlimit]
  rlcompleter: [get_class_members, readline]
  rosdistro: [StringIO, aptdistro, common, develdistro, distribution, distribution_cache,
    distribution_file, doc_build_file, doc_file, doc_repository_specification, external,
    get_cached_distribution, get_cached_release, get_distribution, get_distribution_cache,
    get_distribution_file, get_doc_build_files, get_doc_file, get_index, get_index_url,
    get_release, get_release_build_files, get_release_builds, get_release_cache, get_release_file,
    get_source_build_files, get_source_file, gzip, index, load_url, loader, logger,
    logging, manifest_provider, package, print_function, release, release_build, release_build_file,
    release_cache, release_file, release_repository_specification, repository, repository_specification,
    rosdistro, site_config_dir, source_build_file, source_file, status, user_config_dir,
    walks, yaml]
  rosinstall: [wstool]
  rospkg: [common, environment, expand_to_packages, get_etc_ros_dir, get_log_dir,
    get_package_name, get_ros_home, get_ros_package_path, get_ros_paths, get_ros_root,
    get_stack_version_by_dir, get_test_results_dir, list_by_path, manifest, on_ros_path,
    parse_manifest_file, rospack, stack]
  rsa: [common, core, decrypt, encrypt, key, newkeys, pem, pkcs1, prime, randnum,
    sign, transform, varblock, verify]
  runpy: [get_loader, imp, read_code, run_module, run_path]
  sched: [heapq, namedtuple]
  scikits.audiolab: [aiffread, aiffwrite, auread, auwrite, available_encodings, available_file_formats,
    bench, flacread, flacwrite, i, oggread, oggwrite, play, pysndfile, sdifread, sdifwrite,
    sndfile_version, soundio, supported_encoding, supported_endianness, supported_format,
    test, wavread, wavwrite]
  scipy: [fftpack, ifft, integrate, interpolate, logn, ndimage, optimize, rand, show_numpy_config,
    spatial, special]
  select: [select]
  serial: [TIOCM_DTR_str, TIOCM_RTS_str, TIOCM_zero_str, baudrate_constants, device,
    fcntl, io, plat, portNotOpenError, protocol_handler_packages, select, serial_for_url,
    serialposix, serialutil, set_special_baudrate, struct, termios, time, to_bytes,
    writeTimeoutError]
  setuptools: [bootstrap_install_from, compat, convert_path, depends, dist, distutils,
    extension, find_packages, findall, fnmatchcase, lib2to3_fixer_packages, msvc9_support,
    run_2to3_on_doctests, setup, setuptools, windows_support]
  shedskin: [analyze, annotate, config, cpp, extmod, generate_code, getopt, graph,
    infer, main, makefile, parse_command_line_options, print_errors, python, start,
    struct, time, traceback, typestr, usage, virtual]
  shelve: [Pickler, StringIO, Unpickler]
  shlex: [StringIO, split]
  shutil: [WindowsError, abspath, collections, copy, copy2, copyfile, copyfileobj,
    copymode, copystat, copytree, fnmatch, get_archive_formats, getgrnam, getpwnam,
    ignore_patterns, make_archive, move, register_archive_format, rmtree, unregister_archive_format]
  signal: [alarm, default_int_handler, getitimer, getsignal, pause, set_wakeup_fd,
    setitimer, siginterrupt, signal]
  site: [abs__file__, addpackage, addsitedir, addsitepackages, addusersitepackages,
    aliasmbcs, check_enableusersite, execsitecustomize, execusercustomize, getsitepackages,
    getuserbase, getusersitepackages, makepath, removeduppaths, setBEGINLIBPATH, setcopyright,
    setencoding, sethelper, setquit, traceback]
  sitecustomize: [build_time_vars, e, opt_executable, p, site]
  six: [absolute_import, add_metaclass, add_move, advance_iterator, assertCountEqual,
    assertRaisesRegex, assertRegex, b, byte2int, callable, class_types, create_bound_method,
    exec_, functools, get_function_closure, get_function_code, get_function_defaults,
    get_function_globals, get_method_function, get_method_self, get_unbound_function,
    indexbytes, int2byte, integer_types, iterbytes, iteritems, iterkeys, iterlists,
    itertools, itervalues, moves, operator, print_, python_2_unicode_compatible, raise_from,
    remove_move, reraise, string_types, u, unichr, viewitems, viewkeys, viewvalues,
    with_metaclass]
  skimage: [data_dir, doctest, doctest_verbose, dtype_limits, img_as_bool, img_as_float,
    img_as_int, img_as_ubyte, img_as_uint, pkg_dir, test, test_verbose, util]
  sklearn: [base, clone, externals, re, setup_module, warnings]
  sleekxmpp: [api, basexmpp, clientxmpp, componentxmpp, exceptions, jid, logging,
    plugins, register_stanza_plugin, roster, stanza, thirdparty, util, xmlstream]
  smtpd: [asynchat, asyncore, getopt, parseargs, program, socket, time, usage]
  smtplib: [base64, email, encode_base64, hmac, quoteaddr, quotedata, re, socket,
    ssl, stderr]
  sndhdr: [get_long_be, get_long_le, get_short_be, get_short_le, test, test_8svx,
    test_aifc, test_au, test_hcom, test_sndr, test_sndt, test_voc, test_wav, testall,
    tests, what, whathdr]
  socket: [AF_DECnet, RAND_add, RAND_egd, RAND_status, StringIO, create_connection,
    fromfd, getaddrinfo, getdefaulttimeout, getfqdn, gethostbyaddr, gethostbyname,
    gethostbyname_ex, gethostname, getnameinfo, getprotobyname, getservbyname, getservbyport,
    has_ipv6, htonl, htons, inet_aton, inet_ntoa, inet_ntop, inet_pton, m, meth, ntohl,
    ntohs, p, setdefaulttimeout, socketpair, ssl, warnings]
  sphinx: [addnodes, build_main, directives, domains, errors, locale, main, make_main,
    package_dir, roles, util, version_info]
  sqlite3: [DateFromTicks, TimeFromTicks, TimestampFromTicks, adapt, adapters, apilevel,
    collections, complete_statement, connect, converters, datetime, dbapi2, enable_callback_tracebacks,
    enable_shared_cache, paramstyle, register_adapter, register_converter, sqlite_version,
    sqlite_version_info, threadsafety, time, version_info, x]
  sre_constants: [makedict]
  ssl: [DER_cert_to_PEM_cert, OP_NO_SSLv2, OP_NO_SSLv3, OP_NO_TLSv1, OP_NO_TLSv1_1,
    OP_NO_TLSv1_2, PEM_cert_to_DER_cert, PROTOCOL_SSLv23, PROTOCOL_SSLv3, PROTOCOL_TLSv1,
    PROTOCOL_TLSv1_1, PROTOCOL_TLSv1_2, RAND_add, RAND_egd, RAND_status, base64, cert_time_to_seconds,
    create_connection, create_default_context, get_default_verify_paths, get_protocol_name,
    get_server_certificate, match_hostname, namedtuple, re, sslwrap_simple, textwrap,
    wrap_socket]
  stem: [AuthDescriptorAction, CircBuildFlag, CircClosureReason, CircEvent, CircPurpose,
    CircStatus, Flag, GuardStatus, GuardType, HiddenServiceState, ORClosureReason,
    ORStatus, RelayEndReason, Runlevel, Signal, StatusType, StreamClosureReason, StreamPurpose,
    StreamSource, StreamStatus, TimeoutSetType, prereq, stem, util]
  string: [ascii_letters, ascii_lowercase, ascii_uppercase, atof, atoi, atol, capitalize,
    capwords, center, count, digits, expandtabs, find, hexdigits, index, join, joinfields,
    letters, ljust, lower, lowercase, lstrip, maketrans, octdigits, printable, punctuation,
    replace, rfind, rindex, rjust, rsplit, rstrip, split, splitfields, strip, swapcase,
    translate, upper, uppercase, whitespace, zfill]
  stringprep: [b1_set, b3_exceptions, c22_specials, c6_set, c7_set, c8_set, c9_set,
    in_table_a1, in_table_b1, in_table_c11, in_table_c11_c12, in_table_c12, in_table_c21,
    in_table_c21_c22, in_table_c22, in_table_c3, in_table_c4, in_table_c5, in_table_c6,
    in_table_c7, in_table_c8, in_table_c9, in_table_d1, in_table_d2, map_table_b2,
    map_table_b3, unicodedata]
  subprocess: [check_call, check_output, fcntl, gc, list2cmdline, mswindows, pickle,
    select, signal, traceback]
  sunau: [openfp]
  sys: [api_version, argv, builtin_module_names, byteorder, call_tracing, callstats,
    copyright, displayhook, dont_write_bytecode, exc_clear, exc_info, exc_type, excepthook,
    exec_prefix, executable, exit, flags, float_info, float_repr_style, getcheckinterval,
    getdefaultencoding, getdlopenflags, getfilesystemencoding, getprofile, getrecursionlimit,
    getrefcount, getsizeof, gettrace, hexversion, long_info, maxint, maxsize, maxunicode,
    meta_path, modules, path_hooks, path_importer_cache, platform, prefix, py3kwarning,
    setcheckinterval, setdlopenflags, setprofile, setrecursionlimit, settrace, stderr,
    stdin, stdout, subversion, version_info, warnoptions]
  sysconfig: [get_config_h_filename, get_config_var, get_config_vars, get_makefile_filename,
    get_path, get_path_names, get_paths, get_platform, get_python_version, get_scheme_names,
    is_python_build, parse_config_h]
  syslog: [closelog, openlog, setlogmask, syslog]
  tabnanny: [check, errprint, filename_only, format_witnesses, getopt, main, process_tokens,
    tokenize, verbose]
  tarfile: [bltn_open, calc_chksums, copy, copyfileobj, filemode, filemode_table,
    grp, is_tarfile, itn, nti, nts, operator, pwd, re, shutil, stn, struct, time,
    uts]
  tempfile: [NamedTemporaryFile, TemporaryFile, gettempdir, gettempprefix, mkdtemp,
    mkstemp, mktemp, tempdir, template]
  theanets: [activations, feedforward, graph, layers, main, recurrent, trainer, util]
  theano: [Lop, Rop, Updates, clone, compat, compile, config, configdefaults, configparser,
    dot, foldl, foldr, function, generated_version, generic, get_scalar_constant_value,
    gof, grad, gradient, logging, logging_default_formatter, logging_default_handler,
    map, misc, numpy, opt, pp, pprint, predefined_linkers, predefined_modes, predefined_optimizers,
    printing, reduce, scalar, scan, scan_module, shared, sparse_grad, tensor, test,
    tests, theano, theano_logger, toolbox, updates, utils]
  thread: [allocate, allocate_lock, exit, exit_thread, get_ident, interrupt_main,
    stack_size, start_new, start_new_thread]
  time: [accept2dyear, altzone, asctime, clock, ctime, daylight, gmtime, localtime,
    mktime, sleep, strftime, strptime, time, timezone, tzname, tzset]
  token: [main, tok_name]
  tokenize: [Binnumber, Bracket, Comment, ContStr, Decnumber, Double, Double3, Expfloat,
    Exponent, Floatnumber, Funny, Hexnumber, Ignore, Imagnumber, Intnumber, Name,
    Number, Octnumber, Operator, PlainToken, Pointfloat, PseudoExtras, PseudoToken,
    Single, Single3, Special, String, Token, Triple, Whitespace, any, double3prog,
    endprogs, generate_tokens, group, main, maybe, printtoken, pseudoprog, re, single3prog,
    single_quoted, t, tabsize, tok_name, tokenize, tokenize_loop, tokenprog, triple_quoted,
    untokenize]
  tornado: [absolute_import, division, print_function, version_info, with_statement]
  tox: [cmdline, config, hookimpl, hookspec, hookspecs, interpreters, result, session,
    venv]
  traceback: [extract_stack, extract_tb, format_exc, format_exception, format_exception_only,
    format_list, format_stack, format_tb, linecache, print_exc, print_exception, print_last,
    print_list, print_stack, print_tb, tb_lineno]
  tty: [setcbreak, setraw, tcdrain, tcflow, tcflush, tcgetattr, tcsendbreak, tcsetattr]
  types: [StringTypes]
  unicodedata: [bidirectional, category, combining, decimal, decomposition, digit,
    east_asian_width, lookup, mirrored, normalize, numeric, ucd_3_2_0, ucnhash_CAPI,
    unidata_version]
  unirest: [base64, clear_default_headers, default_header, delete, get, get_parameters,
    gzip, json, multipart_encode, patch, post, put, register_openers, threading, timeout,
    urllib, urllib2]
  unittest: [case, defaultTestLoader, expectedFailure, findTestCases, getTestCaseNames,
    installHandler, loader, makeSuite, registerResult, removeHandler, removeResult,
    result, runner, signals, skip, skipIf, skipUnless, suite, util]
  unittest2: [compatibility, main_]
  urllib: [always_safe, base64, basejoin, c, ftpcache, ftperrors, getproxies, getproxies_environment,
    getproxies_macosx_sysconf, i, localhost, noheaders, pathname2url, proxy_bypass,
    proxy_bypass_environment, proxy_bypass_macosx_sysconf, quote, quote_plus, re,
    reporthook, socket, splitattr, splithost, splitnport, splitpasswd, splitport,
    splitquery, splittag, splittype, splituser, splitvalue, ssl, test1, thishost,
    time, toBytes, unquote, unquote_plus, unwrap, url2pathname, urlcleanup, urlencode,
    urlopen, urlretrieve]
  vcstools: [absolute_import, bzr, common, get_vcs_client, git, hg, logging, print_function,
    register_vcs, setup_logger, svn, tar, unicode_literals, vcs_abstraction, vcs_base]
  virtualenv: [ConfigParser, abiflags, base64, call_subprocess, change_prefix, codecs,
    convert, copy_required_modules, copyfile, copyfileordir, create_bootstrap_script,
    create_environment, default_config_file, default_storage_dir, distutils, expected_exe,
    file_search_dirs, filter_install_output, find_wheels, fix_lib64, fix_local_scheme,
    fixup_egg_link, fixup_pth_and_egg_link, fixup_pth_file, fixup_scripts, get_installed_pythons,
    glob, install_activate, install_distutils, install_python, install_wheel, is_cygwin,
    is_darwin, is_executable, is_executable_file, is_jython, is_pypy, is_win, join,
    logger, logging, mach_o_change, main, majver, make_environment_relocatable, make_exe,
    make_relative_path, maxint, minver, optparse, path_locations, py_version, re,
    read_data, relative_script, resolve_interpreter, rmtree, shutil, strtobool, struct,
    subprocess, subst_path, tarfile, tempfile, user_dir, virtualenv_version, writefile,
    zlib]
  warnings: [default_action, defaultaction, filters, filterwarnings, formatwarning,
    linecache, once_registry, onceregistry, resetwarnings, showwarning, simplefilter,
    warn, warn_explicit, warnpy3k]
  wave: [openfp, struct]
  webbrowser: [get, main, open_new, open_new_tab, register, register_X_browsers, shlex,
    subprocess, time]
  werkzeug: [append_slash_redirect, bind_arguments, check_password_hash, cookie_date,
    create_environ, dump_cookie, dump_header, dump_options_header, escape, exceptions,
    extract_path_info, find_modules, format_string, generate_etag, generate_password_hash,
    get_current_url, get_host, html, http_date, import_string, iri_to_uri, is_entity_header,
    is_hop_by_hop_header, is_resource_modified, make_line_iter, parse_accept_header,
    parse_authorization_header, parse_cache_control_header, parse_cookie, parse_date,
    parse_dict_header, parse_etags, parse_form_data, parse_list_header, parse_options_header,
    parse_set_header, parse_www_authenticate_header, peek_path_info, pop_path_info,
    quote_etag, quote_header_value, redirect, release_local, remove_entity_headers,
    remove_hop_by_hop_headers, responder, routing, run_simple, run_wsgi_app, script,
    secure_filename, test_app, unescape, unquote_etag, unquote_header_value, uri_to_iri,
    url_decode, url_encode, url_fix, url_quote, url_quote_plus, url_unquote, url_unquote_plus,
    validate_arguments, wrap_file, xhtml]
  word2vec: [io, joblib, load, load_clusters, np, scripts_interface, subprocess, unitvec,
    utils, word2clusters, word2phrase, word2vec, wordclusters, wordvectors]
  wstool: [common, config_elements, config_yaml, helpers, ui]
  xdrlib: [raise_conversion_error, struct, wraps]
  xxsubtype: [bench]
  yaml: [add_constructor, add_implicit_resolver, add_multi_constructor, add_multi_representer,
    add_path_resolver, add_representer, compose, compose_all, composer, constructor,
    cyaml, dump, dump_all, dumper, emit, emitter, events, load, load_all, loader,
    nodes, parse, parser, reader, representer, resolver, safe_dump, safe_dump_all,
    safe_load, safe_load_all, scan, scanner, serialize, serialize_all, serializer,
    tokens]
  zlib: [adler32, compress, compressobj, crc32, decompress, decompressobj]
  zmq: [backend, bytes_sockopts, constants, ctx_opt_names, ctx_opts, curve_keypair,
    device, devices, get_includes, has, int64_sockopts, int_sockopts, proxy, pyzmq_version,
    pyzmq_version_info, select, sugar, utils, zmq_errno, zmq_poll, zmq_version, zmq_version_info]
